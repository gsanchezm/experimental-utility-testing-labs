# Agent Governance

| Field | Value |
|---|---|
| Document | protocol/agent-governance-v2.md |
| Protocol version | v2 |
| Protocol state | FROZEN-PRE-DATA |
| Study | Experimental Utility of Software-Testing Laboratory Ecosystems (EUS-2026-001) |
| Created | 2026-09-21 |
| Supersedes | protocol/agent-governance-v1.md (remains FROZEN-PRE-DATA and unedited until this document is itself frozen, per protocol/change-control-v1.md, section 4 — "Rules for the old file after a successor is frozen"; freezing this document is a separate, future, explicitly human-approved action, not performed by this operation) |
| Freeze approval | gilbertosanchez, 2026-09-22 (explicit human approval of this document at content SHA-256 660de9c21ac3e9cb2e7970811248658a81c7f75f7cdd26dd360535a1b7ae9ccf, authorization id AGENT-GOVERNANCE-V2-FREEZE-AUTH-01; post-data-collection change under protocol/change-control-v1.md, section 5; only this header row and the Protocol state row changed at freeze — body text, including its pre-freeze DRAFT narration in section 7, is retained verbatim as a historical snapshot, per the study-design-v1.md section 14 precedent; freeze record manifests/agent-governance-v2-freeze-v1.yaml) |

## What changed relative to v1, and why

Repository history through the E01 freeze established six roles besides ORCHESTRATOR (AUDITOR-SUT01-OMNIPIZZA … AUDITOR-SUT06-WEBDRIVERIO, ADJUDICATOR-EU-01, QUALIFIER-MOBILE-01), none of which is authorized to execute an experimental campaign under experiments/E02–E12 (auditors are scoped to E01 only, by definition and by write boundary; ADJUDICATOR-EU-01 reads audits and writes only derived-data/adjudication/; QUALIFIER-MOBILE-01 is scoped to the mobile runner qualification gate and compatibility smoke, not to any SUT's experimental data). Now that a human has selected E03-resetability as the next campaign for PRE-START preparation (2026-09-21), this version adds the minimum role needed to eventually execute E03, and nothing else:

- **Added:** Section 3.5, role **EXECUTOR-E03-RESETABILITY**.
- **Unchanged:** every role, responsibility, write boundary, and rule from v1 (sections 1, 2, 3.1–3.4, 4 apart from the one added row, 5, 6) is carried forward verbatim in substance; this document restates v1's structure for completeness (frozen documents are never edited in place, so a full restated document is the versioning mechanism, per protocol/change-control-v1.md, section 4) but changes nothing about ORCHESTRATOR, the six auditors, ADJUDICATOR-EU-01, or QUALIFIER-MOBILE-01.
- **Not created:** no generic "EXECUTOR" role for E02 or E04–E12. Each future campaign that needs an executor role gets its own minimum addition, on its own campaign-selection operation, per AGENT-INSTRUCTIONS.md rule 15 and this document's own Section 3.5 scope limit.
- **Not created:** the E03 executor's frozen agent prompt (prompts/frozen/). That is a separate, later step in the campaign-start sequence (prompts/README.md; protocol/change-control-v1.md, section 6, step 6 — applied here at campaign start, not protocol freeze, since protocol v1 is already frozen and this is a campaign-level prompt).

**Revision (same day, 2026-09-21, still DRAFT — no freeze occurred between revisions):** reconciled against the finalized E03 configuration-completion work: (a) the Section 3.5 counting-rule citation now points to `protocol/setup-effort-v2.md` (FROZEN-PRE-DATA), since issue (xviii) was resolved by human ruling `ISSUE-XVIII-RULING-01` in the same operational thread; (b) Section 3.5 gains explicit CI/CD-related permissions and an explicit prohibition on rewriting a previous attempt, matching the reproducibility/attempt model in `experiments/E03-resetability/ci-cd-execution-model.md`; (c) Section 4 gains one new, **proposed, not granted**, write-boundary row for a future CI-config path (`.github/` or equivalent), since no such path is authorized for any role today and this document cannot grant it unilaterally. Still DRAFT throughout; nothing in this revision changes the freeze requirement below.

**Revision (2026-09-22, still DRAFT — no freeze occurred; explicit human decisions of the E03 PRE-START corrective operation, applied and nothing more):** (a) Section 3.5 gains an explicit **READ boundary** for EXECUTOR-E03-RESETABILITY (what it may read; that it must never read another SUT's measured outputs during campaign execution; that common pre-frozen study inputs are readable by every executor); (b) Section 3.5's CI/CD permissions are concretized for **GitHub Actions**, the human-selected E03 orchestrator, with an explicit prohibition on authoring or modifying any workflow and on changing repository settings, runner images, retry, concurrency, pinned runtime versions, or artifact-retention policy, merging pull requests, or approving its own changes; (c) Section 3.5's **WRITE boundary** now lists every path this role must not modify, including `.github/workflows/**`, `research-status.html`, and `AGENT-INSTRUCTIONS.md`; (d) Section 4's CI-config row is no longer "proposed": the `.github/workflows/**` write boundary was **granted to ORCHESTRATOR only** by explicit human authorization `E03-CI-WRITE-AUTH-01` (2026-09-22, recorded in AGENT-INSTRUCTIONS.md, "E03 CI/CD infrastructure"), and this DRAFT carries that grant for incorporation at freeze; (e) references updated to the current authoritative inputs: `experiments/scenario-mappings/CS-001.yaml` is the sole accepted canonical scenario (`CS-002.yaml` EXCLUDED_FROM_CAMPAIGN 2026-09-22, `manifests/e03-cs002-exclusion-v1.yaml`), the per-action outcome field now exists (`schemas/run-manifest.schema.v2.json`; the Section 6 schema-gap escalation is resolved), and controlled-instance status is read from the authoritative controlled-instance record rather than assumed. No E01 role changes; ORCHESTRATOR, the six auditors, ADJUDICATOR-EU-01, and QUALIFIER-MOBILE-01 are unchanged in substance except that ORCHESTRATOR's "May write" list records the `E03-CI-WRITE-AUTH-01` path. This document remains DRAFT and grants nothing until a human freezes its exact content hash.

## 1. Purpose

Unchanged from v1: this document defines the roles under which automated agents and humans operate this repository, what each role is responsible for, and where each role may and may not write, so that no result for any system under test (SUT) can be traced to a role that should not have produced it.

Ten agent roles now exist. Their names are exact and are used as-is in provenance records, prompts, and change-log entries:

ORCHESTRATOR; AUDITOR-SUT01-OMNIPIZZA; AUDITOR-SUT02-SAUCELABS; AUDITOR-SUT03-BROWSERSTACK; AUDITOR-SUT04-RESTFULBOOKER; AUDITOR-SUT05-JUICESHOP; AUDITOR-SUT06-WEBDRIVERIO; ADJUDICATOR-EU-01; QUALIFIER-MOBILE-01; **EXECUTOR-E03-RESETABILITY**.

Humans are not agent roles. Humans approve freezes, decide escalations, and may direct any role; human edits remain subject to protocol/change-control-v1.md.

## 2. Common rules for all agents

Unchanged from v1 (rules 1–8), and apply to EXECUTOR-E03-RESETABILITY exactly as to every other role:

1. Read and follow the root AGENT-INSTRUCTIONS.md before acting.
2. Never invent evidence. Every claim must be backed by an evidence record with its evidence_id (evidence-rules-v1.md), or, for a run, by a run manifest (schemas/run-manifest.schema.json); unknown provenance is null / "null / TBD", never guessed.
3. Never modify a SUT. Observe, execute, and record only.
4. Never change a file whose protocol state is FROZEN-PRE-DATA, AMENDED, or SUPERSEDED; never edit prompts/frozen/ or raw-data/ after the relevant freeze.
5. Record provenance for every artifact written: role name, prompt version id (once E03's prompt is frozen), date, protocol version in force.
6. Do not start any experimental campaign, audit, qualification run, or compatibility smoke unless explicitly instructed by the ORCHESTRATOR under human direction. At this document's creation, E03-resetability is NOT_STARTED and no execution instruction has been given to EXECUTOR-E03-RESETABILITY or any other role.
7. Do not write outside the assigned workspace (Section 3.5, Section 4). A write outside the workspace is a boundary violation, escalated under Section 6, regardless of content.
8. Treat all six SUTs identically. No SUT, including OmniPizza (SUT-01), is a reference, framework, or privileged case.

## 3. Roles

### 3.1 ORCHESTRATOR

Unchanged from v1. See protocol/agent-governance-v1.md, section 3.1, for full text (restated here for completeness of this versioned document): maintains manifests/; pins evaluated versions; creates and versions protocol documents under change control (including this document itself); writes canonical scenario records and mapping entries under experiments/scenario-mappings/ and campaign configuration records; coordinates campaigns E01–E12; maintains provenance; integrates audit and adjudication outputs into derived-data/ and analysis/. May write: manifests/\*\*; protocol/\*\* (in place only while DRAFT; new version files once frozen); experiments/\*\* (including experiments/E03-resetability/, but the "Campaign configuration" heading specifically only at a separately human-authorized campaign start); prompts/\*\*; schemas/\*\*; derived-data/\*\* (version-controlled transformations only); analysis/\*\*; root README.md and AGENT-INSTRUCTIONS.md; `.github/workflows/\*\*` for authorized E03-resetability GitHub Actions infrastructure only (explicit human authorization `E03-CI-WRITE-AUTH-01`, 2026-09-22, recorded in AGENT-INSTRUCTIONS.md; Section 4). Cannot: invent evidence; modify SUTs; alter frozen protocol files in place; edit raw-data/ after campaign freeze; write to audits/\*\* or qualification/\*\*; assign or adjust any capability score; write inside EXECUTOR-E03-RESETABILITY's raw-data/E03-resetability/ run-observation subtree once EXECUTOR-E03-RESETABILITY has begun producing runs there (ORCHESTRATOR coordinates and may write raw-data/ "campaign execution only, under ORCHESTRATOR coordination" per Section 4 below, but does not silently edit or supersede an executor's own run output; a correction follows raw-data/README.md, "Correction records").

### 3.2 Auditors

Unchanged from v1. See protocol/agent-governance-v1.md, section 3.2, for full text; the six roles, their assigned SUTs, and their audits/\<assigned-SUT\>/\*\* write boundary are unaffected by this version.

### 3.3 ADJUDICATOR-EU-01

Unchanged from v1. See protocol/agent-governance-v1.md, section 3.3.

### 3.4 QUALIFIER-MOBILE-01

Unchanged from v1. See protocol/agent-governance-v1.md, section 3.4.

### 3.5 EXECUTOR-E03-RESETABILITY (new in v2)

**Permitted campaign: E03-resetability only.** This role has no authority over E02, E04–E12, or any future campaign; a future campaign that needs an executor role is granted one by its own campaign-selection operation and its own minimum governance addition (this document's own "What changed" section; AGENT-INSTRUCTIONS.md rule 15).

**Responsibility:** on explicit instruction from a human, relayed by the ORCHESTRATOR (Section 2, rule 6), execute E03-resetability runs against exactly one assigned SUT at a time, for the conditions and canonical scenario(s) fixed in E03's locked campaign-configuration record (protocol/change-control-v1.md, section 7) and the FROZEN scenario mapping record(s) it cites (protocol/equivalent-scenario-mapping-v1.md; as of this revision, `experiments/scenario-mappings/CS-001.yaml`, FROZEN, the sole accepted canonical scenario for the current E03 campaign version — `experiments/scenario-mappings/CS-002.yaml` is EXCLUDED_FROM_CAMPAIGN since 2026-09-22, `manifests/e03-cs002-exclusion-v1.yaml`, and is never an input to this role) — never against a DRAFT configuration, a DRAFT mapping record, or an excluded mapping record. Concretely: establish the declared pre-reset (altered) state; perform the reset procedure under evaluation; independently verify pre-reset and post-reset state against the declared known initial state, counting that verification as a Required Action classified by its own mechanism (protocol/setup-effort-v2.md, section 5, rule 8 — human ruling A2); record the ordered Required Actions list (protocol/setup-effort-v2.md, section 4, action classes), including, per action, its SUCCESS / PARTIAL_SUCCESS / FAILURE outcome recorded separately from the count in the run manifest's `actions[].outcome` field (protocol/setup-effort-v2.md, section 5, rule 9 — human ruling B1; `schemas/run-manifest.schema.v2.json`, where the field is optional at schema level and mandatory on every emitted action record by E03 campaign convention; a retry is a further action record with `is_retry` true, never a change to an earlier action's outcome); write one run manifest per run conforming to `schemas/run-manifest.schema.v2.json`, with `reset_mechanisms` mandatory and non-empty; record any manual intervention and its reason; record `environment_type` per run and never execute a deterministic-reset condition against a PUBLIC_HOSTED instance, nor any condition whose controlled instance is not recorded as verified `CONTROLLED` in the authoritative controlled-instance record for E03 (`experiments/E03-resetability/controlled-instance-determination.md`, or the successor record named by the locked campaign configuration; protocol/study-design-v1.md, section 10.3) — a condition recorded `NOT_EXECUTABLE_WITHOUT_CONTROLLED_INSTANCE` is never executed, and a provisioning or environment-verification record is never itself treated as execution authorization.

**GitHub Actions permissions (human decision, 2026-09-22: GitHub Actions is the E03-resetability CI/CD orchestrator; AGENT-INSTRUCTIONS.md, "E03 CI/CD infrastructure", `E03-CI-WRITE-AUTH-01`):** every E03 MEASURED_EXPERIMENT execution runs through the frozen GitHub Actions execution path under `.github/workflows/`; a local execution may only be DEVELOPMENT, SYNTAX_VALIDATION, TOOL_QUALIFICATION, SMOKE, ENVIRONMENT_VERIFICATION, or DRY_RUN and never enters the measured dataset. Within GitHub Actions this role **may**: trigger (`workflow_dispatch`) the already-authorized, version-pinned measured-execution workflow for its own assigned SUT and conditions, supplying only the frozen inputs assigned to it; read that run's own logs and artifacts; and produce its authorized raw-data/artifacts for `raw-data/E03-resetability/<assigned-SUT>/**` through the workflow's defined output step (an immutable artifact bundle per attempt, imported into the repository under ORCHESTRATOR coordination with hash verification — `experiments/E03-resetability/ci-cd-execution-model.md`). This role **may not**: author, edit, or approve any workflow definition (`.github/workflows/**` is ORCHESTRATOR-only, Section 4); modify GitHub repository settings; change runner images; change retry policy; change concurrency policy; change pinned runtime versions; change artifact-retention policy; merge pull requests; or approve its own changes. It never disables, reconfigures, or overrides a platform-level retry, timeout, or concurrency policy at all; if a platform-native re-run occurs, the GitHub run id and run attempt are mapped explicitly onto the study attempt lineage in the new attempt's run manifest, and the earlier attempt is left untouched.

**Attempt immutability (explicit):** this role never rewrites, replaces, edits, or deletes a previous attempt's run manifest or artifacts, under any circumstance, including a CI-native automatic retry — a further actual attempt is always a new run manifest with `attempt` incremented (schemas/run-manifest.schema.json), never a mutation of the prior one (Section on "Immutable first-run output policy," below, generalizes to every attempt, not only the first).

**READ boundary (explicit):** EXECUTOR-E03-RESETABILITY **may read**: the authoritative frozen E03 campaign configuration (the locked "Campaign configuration" record); the accepted, frozen canonical scenario definition (`experiments/scenario-mappings/CS-001.yaml`); the accepted, frozen mapping entry applicable to its assignment (its own assigned SUT's entry in that record); the version-pinned workflow definition it is authorized to trigger; the explicitly assigned common frozen study inputs (protocol documents in force, `schemas/run-manifest.schema.v2.json`, `manifests/sut-manifest.yaml`, `manifests/toolchain-manifest.yaml`, the provisioning and controlled-instance records for its own assigned SUT, and its own frozen executor prompt) — common pre-frozen study inputs are readable by every executor instantiation; the artifacts, source, and configuration of its own assigned SUT where its assigned conditions require them; and its own prior execution and attempt lineage under `raw-data/E03-resetability/<assigned-SUT>/**` where the frozen execution protocol requires it (for example to assign the next attempt number). It **must not read**, during campaign execution: the experimental measured outputs of any other SUT (`raw-data/E03-resetability/<other-SUT>/**`, any other SUT's run manifests, action records, artifacts, or environment-verification results produced as measured data), any derived or analysed E03 result, or any comparison across SUTs. Cross-SUT measured-result contamination is forbidden and is a boundary violation escalated under Section 6, regardless of whether the read changed anything. In GitHub Actions this isolation is enforced structurally: the measured-execution workflow checks out only the common frozen inputs and the assigned SUT's own `raw-data/E03-resetability/<assigned-SUT>/` subtree, never another SUT's subtree.

**WRITE boundary (explicit):** this role writes only `raw-data/E03-resetability/<assigned-SUT>/**` (the "May write" paragraph below), unless another explicit E03 execution-output path already exists and is separately authorized in the locked campaign configuration. It must not modify, during execution: `protocol/**`, `manifests/**`, `experiments/**`, `schemas/**`, `.github/workflows/**`, `audits/**`, `derived-data/**`, `analysis/**`, `prompts/**`, `qualification/**`, `research-status.html`, `AGENT-INSTRUCTIONS.md`, root `README.md` or `.gitignore`, or any other SUT's `raw-data/` tree.

**Assigned SUT/surface/condition isolation:** EXECUTOR-E03-RESETABILITY is instantiated once per assigned SUT, named in the explicit instruction that launches it (for example: "EXECUTOR-E03-RESETABILITY, assigned SUT-01"). Each instantiation acts on its assigned SUT's conditions only, for the surfaces and canonical scenario(s) that SUT is marked COMPARABLE on in the relevant FROZEN mapping record. An instantiation never reads, writes, references, or draws on another SUT's raw observations, run manifests, or artifacts, mirroring the auditor isolation model (protocol/agent-governance-v1.md, section 3.2) applied to campaign execution rather than audit.

**No cross-SUT contamination:** this role never compares, ranks, or references one SUT's outcome while executing another's runs; it does not state or imply a preference between SUTs; a run manifest or note referencing a different SUT's result is a boundary violation escalated under Section 6.

**May write:** only `raw-data/E03-resetability/<assigned-SUT>/**` (layout per raw-data/README.md: one subdirectory per run, `run-manifest.json` plus raw outputs; `<assigned-SUT>` fixed for the lifetime of the instantiation), including `raw-data/E03-resetability/<assigned-SUT>/unresolved.md` for its own escalations (Section 6), following the same workspace-local escalation-log pattern as `audits/<SUT>/unresolved.md`.

**Immutable output policy (every attempt, not only the first):** once any attempt's raw observation directory is written under `raw-data/E03-resetability/<assigned-SUT>/<run-id>/`, it is never edited, renamed, or deleted (raw-data/README.md, rules 1–2), even by this role, even to fix an error, and even when a CI platform's own native retry produced it; a correction is a new derived record under derived-data/ (ORCHESTRATOR-authored, raw-data/README.md, "Correction records"), never an edit here. Once the E03 campaign is frozen (protocol/change-control-v1.md, section 7), `raw-data/E03-resetability/` as a whole becomes append-only and a further run may be appended only with its own change-log row stating the reason.

**Cannot:** invent evidence or fabricate a Required Actions count or a per-action outcome (Section 2, rule 2; this role's Responsibility paragraph); modify a SUT (Section 2, rule 3); write protocol/\*\*, manifests/\*\*, experiments/\*\* (including experiments/scenario-mappings/ and any campaign configuration record), audits/\*\*, qualification/\*\*, derived-data/\*\*, analysis/\*\*, prompts/\*\*, `.github/workflows/\*\*` (Section 4 — ORCHESTRATOR-only under `E03-CI-WRITE-AUTH-01`), `research-status.html`, `AGENT-INSTRUCTIONS.md`, or any other SUT's `raw-data/E03-resetability/` subtree; read another SUT's measured outputs during campaign execution (READ boundary above); **adjudicate** — this role never confirms, disputes, or resolves a capability score or an evidence conflict (that is ADJUDICATOR-EU-01's exclusive function, Section 3.3, scoped to E01 only in any case); **analyze hypotheses** — this role never computes, states, or implies a result for H1–H5, a comparison outcome, an effect size, or a confidence interval (that is ORCHESTRATOR/analysis/ work under protocol/statistical-analysis-plan-v1.md, after campaign freeze); **select a campaign** — this role has no say in which campaign runs next, whether E03 starts, or whether E03 is frozen; those are ORCHESTRATOR-proposed, human-approved actions (protocol/change-control-v1.md, section 7); **modify protocol** — this role cannot create, edit, version, or interpret away any protocol document; execute against a DRAFT campaign configuration, a DRAFT scenario mapping record, or an excluded mapping record (`CS-002.yaml`); execute a condition not recorded as verified `CONTROLLED` in the authoritative controlled-instance record (`experiments/E03-resetability/controlled-instance-determination.md` or its named successor); execute any MEASURED_EXPERIMENT outside the frozen GitHub Actions execution path; execute an Android Native or iOS Native condition before the mobile runner has passed the qualification gate and the assigned SUT has passed its own compatibility smoke (protocol/mobile-runner-policy-v1.md, sections 2, 9); rewrite, replace, or delete a previous attempt's output, including one produced by a CI-native retry.

**Relationship to the future frozen executor prompt:** this role's concrete agent prompt does not yet exist (prompts/working/ and prompts/frozen/ contain no E03 executor prompt at the time of this document). Prompt authoring and freeze (prompts/README.md, rule 2; protocol/agent-governance-v1.md, section 5) are a separate, later step in the E03 campaign-start sequence, not performed by this document or this operation. Once written and frozen, that prompt implements — and may not contradict or loosen — every constraint in this section; a conflict between a future prompt and this section is a boundary violation, not a prompt-governs-over-governance exception.

## 4. Write-boundary matrix

Unchanged from v1 for every existing role; one new column added.

| Location | ORCHESTRATOR | AUDITOR-SUT0X (each) | ADJUDICATOR-EU-01 | QUALIFIER-MOBILE-01 | EXECUTOR-E03-RESETABILITY |
|---|---|---|---|---|---|
| `protocol/` | Allowed (in place while DRAFT; new version files only once frozen) | — | — | — | — |
| `manifests/` | Allowed | — | — | — | — |
| `audits/SUT-0X/` | — | Allowed for own assigned SUT only; — for all others | — | — | — |
| `qualification/` | — | — | — | Allowed | — |
| `experiments/` (incl. `experiments/scenario-mappings/` and campaign configuration records) | Allowed | — | — | — | — |
| `raw-data/E01-capability-audit/` (unused; E01 is an audit) | Campaign execution only, under ORCHESTRATOR coordination | — | — | — | — |
| `raw-data/E03-resetability/<assigned-SUT>/` | Campaign execution only, under ORCHESTRATOR coordination (does not silently edit or supersede executor output; Section 3.1) | — | — | — | Allowed for own assigned SUT only; — for all others |
| `raw-data/` (E02, E04–E12) | Campaign execution only, under ORCHESTRATOR coordination | — | — | — | — (out of scope, Section 3.5) |
| `derived-data/` | Allowed (version-controlled transformations only) | — | `derived-data/adjudication/` only | — | — |
| `analysis/` | Allowed | — | — | — | — |
| `prompts/` | Allowed (`prompts/frozen/` write-once per version id) | — | — | — | — |
| `schemas/` | Allowed | — | — | — | — |
| `.github/workflows/**` (the only CI-config path) | **Allowed — GRANTED by explicit human authorization `E03-CI-WRITE-AUTH-01`** (2026-09-22; recorded in AGENT-INSTRUCTIONS.md, "E03 CI/CD infrastructure"), for authorized E03-resetability GitHub Actions infrastructure only (validation, provisioning / environment verification, mobile-qualification preparation, gated measured execution). This DRAFT carries the grant for incorporation at freeze; the grant itself is in force through AGENT-INSTRUCTIONS.md, not through this DRAFT. No other path under `.github/` is authorized for any role. | — | — | — | — (trigger/read only; never author, edit, or approve a workflow; Section 3.5) |
| root `README.md`, `AGENT-INSTRUCTIONS.md`, `.gitignore` | Allowed | — | — | — | — |

`raw-data/` is never written directly by any role as an editing action; it is populated only by the execution of a campaign defined in `experiments/`, coordinated by the ORCHESTRATOR, and becomes append-only once that campaign is frozen (unchanged from v1).

## 5. Prompt governance

Unchanged from v1. See protocol/agent-governance-v1.md, section 5. EXECUTOR-E03-RESETABILITY's prompt, once authored and frozen, follows the identical rules: frozen with a version id before use, never edited in place, every artifact it produces records that version id.

## 6. Escalation

Unchanged from v1 in substance; the workspace-local escalation-log location for EXECUTOR-E03-RESETABILITY is `raw-data/E03-resetability/<assigned-SUT>/unresolved.md` (Section 3.5), mirroring the audit-directory pattern. The following are never resolved by an agent on its own authority, including EXECUTOR-E03-RESETABILITY: conflicts between this role's instructions and the frozen protocol as written (for example, being instructed to execute against a DRAFT mapping record, a condition not yet verified `CONTROLLED`, or a schema that cannot represent a required field — the per-action outcome gap of `experiments/E03-resetability/ci-cd-execution-model.md`, section 6, is resolved by `schemas/run-manifest.schema.v2.json`, 2026-09-22, and is cited here only as the worked example of this class); write-boundary violations; any situation in which following one protocol document would violate another. Each is recorded in the noticing role's own workspace unresolved.md; when it concerns a protocol document or a frozen file, the ORCHESTRATOR mirrors it into `protocol/unresolved.md` (row ids `PROTO-U0n`). Humans decide the outcome.

## 7. Versioning

This is protocol/agent-governance-v2.md, version v2, state DRAFT. It was created 2026-09-21 during E03-resetability PRE-START preparation, under human authorization to determine "the minimum agent-governance-v2.md necessary to authorize E03 execution," and revised the same day (still DRAFT) to reconcile it with the finalized E03 configuration-completion work (canonical-scenario catalogue, CI/CD design, controlled-instance determination), and again on 2026-09-22 (still DRAFT) to apply the human decisions of the E03 PRE-START corrective operation (explicit READ and WRITE boundaries, GitHub Actions permissions and prohibitions, the granted `.github/workflows/**` boundary, CS-001 as sole accepted scenario, schema v2). Creating and revising a DRAFT file requires no separate approval (protocol/change-control-v1.md, section 2, state-transition table: "(new file) -> DRAFT: No approval required"; "DRAFT -> DRAFT: In-place edit before freeze: No approval required"). Freezing it (DRAFT -> FROZEN-PRE-DATA, or -> AMENDED since `data_collection_started` is already true) is a distinct action requiring its own explicit human approval and, per protocol/change-control-v1.md, section 5 (post-data-collection changes), a written rationale, an explicit list of affected campaigns, and a change-log entry; none of that approval is given or assumed by this operation, and this document does not self-approve its own freeze.

**`HUMAN_DECISION_REQUIRED — AGENT-GOVERNANCE-V2 FREEZE`**: approve or decline freezing this file as written. Artifact to approve: `protocol/agent-governance-v2.md` at the exact commit that introduces it (a file cannot record its own content hash inside itself without invalidating that hash the moment it is written — see the accompanying operation's provenance manifest and return package for the SHA-256 of this exact file content). Approving this freeze does not, by itself, launch any instance of EXECUTOR-E03-RESETABILITY (Section 3.5's own instruction-based launch rule, Section 2 rule 6, is unaffected by this document's freeze state), does not start E03, and does not authorize mobile qualification execution or an executor prompt freeze; the `.github/workflows/**` write boundary (Section 4) was granted separately by `E03-CI-WRITE-AUTH-01` and this freeze merely incorporates it. The exact SHA-256 of this file's content at the commit that introduces this revision is the artifact to approve; it is recorded in the operation's provenance manifest and return package, never inside this file.

Until frozen, this document authorizes nothing: protocol/agent-governance-v1.md remains the sole agent-governance document in force, EXECUTOR-E03-RESETABILITY is a proposed role only, and no instance of it may be launched under this DRAFT.
