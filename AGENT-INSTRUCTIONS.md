# AGENT-INSTRUCTIONS.md — Project Instructions for All Agents

These instructions are binding for every automated agent (and human operator) working in this repository; they override default behavior.

Study: Experimental Utility of Software-Testing Laboratory Ecosystems (EUS-2026-001). Protocol version v1. A system under test (SUT) is one of the six evaluated ecosystems listed in README.md. Repository root: the working directory (README.md, section 10).

## Rules

1. **This repository is scientific experimental infrastructure.** Every file is protocol, provenance, evidence, or a reproducible transformation of evidence; edit it with the care of a lab notebook.
2. **Never change frozen protocol files silently.** A file in state FROZEN-PRE-DATA or later is never overwritten; a modification creates a new version file and a change-log entry per protocol/change-control-v1.md.
3. **Never modify a SUT to make it score better.** An ecosystem is evaluated as found at its recorded commit or release; forks, patches, or configuration changes that alter observed capability invalidate the comparison.
4. **Never infer that a vendor's commercial testing product gives its demo SUT the same capability.** Only the evaluated ecosystem counts as evidence; a vendor's cloud or product features are not evidence about the demo ecosystem (protocol/evidence-rules-v1.md).
5. **Never assign comparative conclusions without recorded evidence.** Every claim about an ecosystem traces to an evidence record, referenced by its evidence_id (never by CSV row or line number), carrying one of the evidence statuses DOCUMENTED, SOURCE_VERIFIED, EXECUTION_VERIFIED, INFERRED (protocol/evidence-rules-v1.md).
6. **Never delete raw data.** raw-data/ is append-only after campaign freeze (raw-data/README.md).
7. **Raw observations must be immutable once a campaign is frozen.** Corrections are recorded as correction records or new derived datasets, never as edits to raw files.
8. **Derived results must always be reproducible from raw data.** Every derived dataset names its raw inputs, transformation script, and script version (derived-data/README.md, analysis/README.md).
9. **Every experimental run must record provenance.** A run manifest conforming to schemas/run-manifest.schema.json accompanies every run and identifies, at minimum, the campaign, the SUT, the protocol version, the environment_type, the tool versions (null only with a justification in notes), and the prompt_version used (null only for a human-executed run, per prompts/README.md rule 5). In E01, only EXECUTION_VERIFIED evidence requires this, as an execution verification record (protocol/evidence-rules-v1.md).
10. **Every SUT must be evaluated using the same applicable rules.** The same rubric (protocol/capability-rubric-v1.md), evidence rules, and procedures apply to all six ecosystems; a modality that does not apply is recorded as such, never skipped silently.
11. **Specialist SUTs must not be penalized for intentionally narrow scope.** SUT-04, SUT-05, and SUT-06 are baselines in their specialty; the profile is per-dimension, and there is no composite score in which narrow breadth could be traded against depth. A structurally absent surface is Score 0 with a structural_scope_note, never N/A; Experimental Breadth and Modality Depth answer different questions (protocol/study-design-v1.md, section 11).
12. **OmniPizza receives no privileged interpretation.** SUT-01 is one evaluated SUT, not the framework, the reference, or an expected winner; identical rules and identical evidence thresholds apply to it.
13. **Any protocol change after data collection begins requires: new protocol version; rationale; affected campaigns; explicit record in change log.** Procedure: protocol/change-control-v1.md.
14. **Mobilewright is the candidate mobile runner. Appium 3 is the fallback. Do not introduce Appium 2.** Mobilewright must pass the qualification gate (MQ1, MQ2, MQ3) before experimental use (protocol/mobile-runner-policy-v1.md). No mobile runner is selected: the only gate executed (2026-09-22) ran without a valid human authorization and is quarantined, and the Appium 3 selection derived from it is superseded (protocol/unresolved.md, PROTO-U10; manifests/mobile-qualification-authorization-correction-v1.yaml).
15. **Do not start experiments unless explicitly instructed.** No audit, qualification run, campaign, tool installation, or SUT clone happens on an agent's own initiative.
16. **Do not push changes outside the assigned agent workspace without explicit instruction.** Write boundaries per role are defined in protocol/agent-governance-v2.md (FROZEN-PRE-DATA 2026-09-22; v1 AMENDED, superseded by v2).

## Current phase

- Phase: SETUP (manifests/study-manifest.yaml, `status`; repository initialized 2026-09-15; protocol v1 pre-data freeze instructed and completed 2026-09-16). This top-level phase label has not been renamed since freeze even though substantial work has since occurred within it (below) — SETUP is the literal current value of `manifests/study-manifest.yaml`, not a claim that nothing has happened.
- Protocol state: **FROZEN-PRE-DATA** for every document under protocol/ (approved by gilbertosanchez, 2026-09-16; protocol/change-control-v1.md, section 6; provenance in manifests/protocol-freeze-v1.yaml and protocol/CHANGELOG.md). The Open-Issue Register (protocol/study-design-v1.md, section 13) is **live status RESOLVED 19, OPEN 0, DEFERRED-BEFORE-CAMPAIGN 1** (manifests/study-manifest.yaml, `open_issue_register`) — issue (xviii) was resolved 2026-09-21 by explicit human ruling (authorization id ISSUE-XVIII-RULING-01: verification-as-action = A2, partial-success counting = B1; protocol/setup-effort-v2.md, now FROZEN-PRE-DATA, superseding protocol/setup-effort-v1.md, now AMENDED; provenance manifests/issue-xviii-ruling-v1.yaml). Only issue (xix) remains deferred, to E04/E05's own configuration. The Register's own table text in protocol/study-design-v1.md, section 13, is **not** edited in place for this (a frozen document's row text is a historical snapshot, not live status — same rule as the section-14 snapshot noted below); this bullet and manifests/study-manifest.yaml are the live status. Every document under protocol/ is now immutable in place except for a non-substantive correction (protocol/change-control-v1.md, section 2) or a new versioned successor file.
- data_collection_started: **true** (manifests/study-manifest.yaml; became true 2026-09-17 at E01 campaign start, by explicit human approval, per protocol/change-control-v1.md, section 7).
- E01 Capability Audit: campaign status **FROZEN** (manifests/study-manifest.yaml; started IN_PROGRESS 2026-09-17, frozen 2026-09-18T20:01:45Z under explicit human authorization E01-FREEZE-AUTH-01 — protocol/change-control-v1.md, section 7; full record manifests/e01-campaign-freeze-v1.yaml). Within it, complete and committed: all six independent SUT audits; the independent single-run adjudication (ADJUDICATOR-EU-01); both human rulings on the two DISPUTED cells (E01-HR-01, E01-HR-02); the corrected final adjudicated capability matrix (PROTO-U06, `derived-data/e01-adjudicated-matrix/E01-adjudicated-matrix.json`); and the H1 confirmatory analysis (`derived-data/h1-analysis/H1-result.json`) — a descriptive count comparison only, per protocol/statistical-analysis-plan-v1.md, section 4.3; no support/non-support criterion is frozen for H1, and none was invented. From this freeze, E01's scientific inputs/results are closed to ordinary modification (protocol/change-control-v1.md, section 7); any further change follows section 5 in full.
- Every campaign remains **NOT_STARTED** (manifests/study-manifest.yaml) — including E03-resetability, human-selected for **PRE-START preparation only** (2026-09-21) and, on 2026-09-22, for **NON-MEASURED PRE-START provisioning and environment verification** of the CS-001 conditions (explicit human authorization; records are labeled PROVISIONING or ENVIRONMENT_VERIFICATION, never MEASURED_EXPERIMENT). Outcome: 8 of 9 conditions have a verified controlled instance on the operator workstation (LOCAL / EMULATED) and `E03-CS001-SUT02-IOS` does not (host simulator service unresponsive); `controlled_instance_available` is true for SUT-03, SUT-04, SUT-05 and null (never inferred) for SUT-01, SUT-02, SUT-06; no condition is measured-execution-ready (the GitHub Actions environment verification has not run) — `manifests/provisioning/`, `experiments/E03-resetability/controlled-instance-status.yaml`, `controlled-instance-determination.md`. E03 remains NOT_STARTED and none of E02–E12 has a campaign-configuration record. Mobile runner qualification: **INVALID / REQUIRES RE-EXECUTION** (protocol/unresolved.md, **PROTO-U10**, 2026-09-22; manifests/mobile-qualification-authorization-correction-v1.yaml). A Mobilewright gate was executed on 2026-09-22 through `.github/workflows/e03-mobile-qualification.yml` (runs 35763051534 Android, 35767911878 iOS; 6 warm-ups, 60 measured executions) on the ORCHESTRATOR's relay of `MOBILE-QUALIFICATION-EXEC-AUTH-01`, a record that **no human had issued** (the human research lead has determined that the text was a proposed draft never approved; no artifact predating the record carries a human authorization). protocol/mobile-runner-policy-v1.md section 2's explicit-human-instruction prerequisite was therefore not satisfied: the executions are preserved unchanged under qualification/mobilewright/output/ and classified **HISTORICAL_UNAUTHORIZED_EXECUTION** (observed but protocol-inadmissible for runner-selection authority); the FAIL decision recorded that day and the switch-rule selection of Appium 3 derived from it are **superseded** (section 8 was not legitimately triggered). Historical, non-authoritative observation: Mobilewright 0.0.60 passed MQ1/MQ2 on both platforms and MQ3 on iOS 10/10 each, and failed MQ3 on the Android emulator 10/10 (MC-07: the deep link's route was delivered, its query parameters were not). **Current valid state:** primary candidate Mobilewright 0.0.60, fallback Appium 3.7.0, **selected mobile runner none**; Mobilewright `qualification_status` INVALID_EXECUTION_AUTHORIZATION (a protocol-valid gate is NOT_STARTED and requires a **NEW** explicit human authorization with a new id; the retired id is refused by the workflow's readiness gate); Appium 3 not installed, its compatibility smoke **NOT AUTHORIZED / NOT_STARTED**; Appium 2 never (rule 14). **PREQUALIFICATION_OUTCOME_EXPOSURE = true**: the qualification SUT, the Mobilewright version, the qualification build, MQ1–MQ3, MC-01…MC-10, N = 10, the pass criteria, the attribution rules, and the fallback rule are immutable in response to the exposed outcome; the realization/harness hashes for any repeat are in the correction record. A `LOCAL_DEVELOPMENT_PREFLIGHT` class (non-qualification, non-measured, not part of N; Android physical device, API 35 emulator, Xcode Simulator) is prepared for a separate human instruction. Qualification records are tool-qualification records, never evidence about any SUT. protocol/agent-governance-v2.md was **FROZEN 2026-09-22** at its exact pre-freeze content hash (AGENT-GOVERNANCE-V2-FREEZE-AUTH-01; manifests/agent-governance-v2-freeze-v1.yaml); it defines EXECUTOR-E03-RESETABILITY and authorizes no execution. `experiments/scenario-mappings/`: `CS-001.yaml` (FROZEN 2026-09-21, E03-CS001-FREEZE-AUTH-01) is the **sole accepted canonical scenario** for the current E03 campaign version; `CS-002.yaml` (FROZEN 2026-09-21) was **EXCLUDED_FROM_CAMPAIGN on 2026-09-22** by explicit human ruling `E03-CS002-EXCLUSION-RULING-01` — its intent was generalized from SUT-06-specific E01 evidence, contrary to protocol/equivalent-scenario-mapping-v1.md, section 6 — with the file preserved unchanged and no successor or replacement record; it is not an input to any E03 configuration, workflow, or executor (manifests/e03-cs002-exclusion-v1.yaml; protocol/CHANGELOG.md; protocol/unresolved.md, PROTO-U09; experiments/E03-resetability/canonical-scenario-catalogue.md). Canonical scenarios are defined independently of SUT-specific implementation evidence; E01 evidence establishes mapping/comparability only. SUT-06 remains in the six-SUT population (NOT_COMPARABLE on CS-001; no measured condition in the current configuration; none invented). Active candidate conditions: 9, all on CS-001. A FROZEN mapping record per scenario remains the hard prerequisite for E02–E07 execution (experiments/README.md, "Common rules for all campaigns"), independent of and in addition to the explicit-human-authorization requirement below. Issue (xviii) is RESOLVED (A2+B1, ISSUE-XVIII-RULING-01, protocol/setup-effort-v2.md); PROTO-U08 is RESOLVED (DUPLICATE_OR_ALREADY_RESOLVED_CONCERN, non-blocking, resolved by the frozen protocol itself). `schemas/run-manifest.schema.v2.json` **exists** (2026-09-22; adds only the optional `actions[].outcome` field, SUCCESS / PARTIAL_SUCCESS / FAILURE; v1 preserved unchanged; manifests/run-manifest-schema-v2-provenance.yaml): future E02/E03 run manifests conform to v2, and `actions[].outcome` is mandatory on every emitted action record by campaign convention. **GitHub Actions is the selected E03 CI/CD orchestrator** (human decision 2026-09-22; "E03 CI/CD infrastructure" below). `protocol/agent-governance-v2.md` is **FROZEN-PRE-DATA** (2026-09-22, at its exact pre-freeze content hash, AGENT-GOVERNANCE-V2-FREEZE-AUTH-01; defines role EXECUTOR-E03-RESETABILITY; authorizes no execution). The public GitHub repository (https://github.com/gsanchezm/experimental-utility-testing-labs, PUBLIC) **was published on 2026-09-22** as a **provider-neutral clean reproducibility snapshot** (publication mode CLEAN_PROVIDER_NEUTRAL_REPRODUCIBILITY_EXPORT, human decision 2026-09-22): public `main` plus the six `audit/e01-sut0N-*` snapshot branches; the canonical history is preserved internally and never rewritten, and no canonical commit object is reachable from the public repository; every exported branch carries a PUBLIC-EXPORT-MANIFEST.yaml tracing each file to its canonical blob; the pre-push zero-trace scan and the public validation workflow's hygiene job both PASS; the export log (canonical source commits, snapshot commits, trees, manifest hashes, validation-run ids) lives in `PUBLIC-EXPORT-MANIFEST.yaml` (the canonical `origin` push URL stays disabled; publication happens only from a separate export repository built from archived canonical trees). The four E03 workflows are published; only the validation workflow has run (on push); no environment-verification, mobile-qualification, or measured-execution run exists. No frozen artifact designates E03, or any campaign, as formally "started". A LOCAL_DEVELOPMENT_PREFLIGHT was executed 2026-09-22 by explicit human instruction: implementation baseline locked (manifests/mobile-qualification-implementation-lock-v1.yaml, MOBILE-QUALIFICATION-IMPLEMENTATION-LOCK-01, all eleven realization/harness hashes recomputed and matched); Android on a physical device (Samsung Galaxy Z Flip 6) COMPLETE (MQ1/MQ2 reached their terminal state; MQ3 delivered the route, the seeded cart was not rendered — same terminal state as the quarantined emulator observation, no inference drawn about it); iOS on the operator's Xcode Simulator BLOCKED (HOST_SIMULATOR_SERVICE_UNRESPONSIVE, a pre-scenario infrastructure failure, six timed-out `simctl` probes). A human-authorized narrow retry of the iOS-only local preflight, run later the same day (2026-09-22), was **BLOCKED again** at the bounded CoreSimulator health-check gate (3/3 bounded 60s `simctl` probes timed out); the implementation lock was reverified and remained byte-identical; evidence shows the same CoreSimulatorService process from the first attempt was still running, never restarted, past 12 hours of uptime, under host load at least as high as before. No MQ1/MQ2/MQ3 was executed on either iOS attempt. By human decision the same day, the **LOCAL_DEVELOPMENT_PREFLIGHT phase is now CLOSED** (qualification/mobilewright/local-preflight/LOCAL-PREFLIGHT-CLOSURE.yaml): no further local execution (iOS retry, further Z Flip 6 runs, or CoreSimulator repair) will be performed; the Android scratchpad driver is classified `LOCAL_DEVELOPMENT_PREFLIGHT_ADAPTER` (not part of the implementation baseline; the future formal gate must not use it); and the local iOS CoreSimulator blocker is explicitly NOT a prerequisite for formal qualification, which runs on the pinned GitHub-hosted macOS substrate, not the operator's Mac. This closure does not authorize formal qualification. **MOBILE-QUALIFICATION-EXEC-AUTH-02** was issued by the human research lead on 2026-09-23 (authorization provenance VALID) and was **never dispatched**: the ORCHESTRATOR's pre-dispatch check found three pre-dispatch execution-integrity / provenance defects (formal output written into the quarantined `qualification/mobilewright/output/`; Android and iOS able to run concurrently against the shared qualification account; active prompt version hardcoded as v1), and by explicit human instruction AUTH-02 was **superseded before dispatch** (**PROTO-U12**; manifests/mobile-qualification-auth-02-dispatch-supersession-v1.yaml; the readiness gate refuses it). Corrected before any execution: formal output isolated in `qualification/mobilewright/formal/<authorization id>/` (absent or empty before execution), one dispatch with order gate → Android → iOS, active prompt version taken from the authorization record; successor lock manifests/mobile-qualification-implementation-lock-v2.yaml and package manifests/mobile-qualification-package-v2.yaml (v1 of each immutable history; scenario realization, runner calls, deep-link mechanism, Mobilewright 0.0.60, N, criteria, attribution, switch rule unchanged). The next authorization, **MOBILE-QUALIFICATION-EXEC-AUTH-03**, was **ISSUED on 2026-09-23** by explicit human instruction, confirmed directly by the human in the same session (record promoted in place: `record_validity: VALID`, `status: ISSUED`, `authorizes_execution: true`, `dispatch_eligibility: ELIGIBLE`; manifests/mobile-qualification-execution-auth-v3-prepared.yaml, filename suffix historical; frozen launch prompt prompts/frozen/qualifier-mobile-01-mobilewright-gate-v3.md, rendered only `INSTRUCTION_DATE` = 2026-09-23) and is **NOT DISPATCHED**: Mobilewright qualification is AUTHORIZED_BUT_NOT_DISPATCHED, no formal qualification data exists, and no runner is selected. Issuance is not dispatch — one formal dispatch needs its own separate explicit human instruction; a drafted or prepared authorization is never itself an authorization. Full records: qualification/mobilewright/local-preflight/ (first attempt), qualification/mobilewright/local-preflight/ios-simulator-retry/ (retry), qualification/mobilewright/local-preflight/LOCAL-PREFLIGHT-CLOSURE.yaml (closure). This preflight is NON_QUALIFICATION, NON_MEASURED, NOT_PART_OF_N, NOT_PASS_FAIL_EVIDENCE, NOT_RUNNER_SELECTION_EVIDENCE, and NOT_E03_DATA; it changes no qualification status, selects no runner, and does not weaken PREQUALIFICATION_OUTCOME_EXPOSURE's immutability list. Separately, protocol/agent-governance-v2.md's frozen header carries a representation-only erratum (PROTO-U11, manifests/agent-governance-v2-header-erratum-v1.yaml): its `Supersedes` row retains stale pre-freeze narrative wording (the freeze is validly performed; only that row's prose is historical). Remaining human approvals for E03: review of the AUTH-03 issuance and static readiness, then a separate explicit instruction authorizing one formal dispatch of the protocol-valid gate under MOBILE-QUALIFICATION-EXEC-AUTH-03; then, depending on its outcome, the per-SUT compatibility smoke of the selected runner for SUT-02 (Android, iOS); E03 campaign START; executor prompt freeze and launch.
- No new experiment, campaign, tool installation, qualification run, or SUT clone begins without explicit human instruction (rule 15) — this remains unchanged and unweakened by anything above. H2–H5 have not been analyzed.
- `protocol/study-design-v1.md`, section 14, still reads "Data collected: none", "data_collection_started is false", and "Results: none": that text is a **HISTORICAL PRE-DATA STATE SNAPSHOT**, correct as of the 2026-09-16 protocol freeze and preserved in place per that document's FROZEN-PRE-DATA immutability (protocol/change-control-v1.md, section 2) — it is **not** current operational status. Current status always lives in `manifests/study-manifest.yaml` and this "Current phase" section, never in that frozen snapshot.

### Dashboard location — resolved (DASHBOARD-ROOT-AUTH-01)

`research-status.html` lives at the repository root under an explicit, narrow human authorization (DASHBOARD-ROOT-AUTH-01, 2026-09-18): this specific file, at this specific path, is authorized to exist and be updated at root. This is **not** a general authorization to create arbitrary root files, and it is not scientific protocol, methodology, evidence, or statistical input — it is a file-specific operational authorization only. protocol/agent-governance-v1.md's write-boundary matrix (section 4) is unchanged by this authorization: the root row still names only `README.md`, `AGENT-INSTRUCTIONS.md`, `.gitignore`, and ORCHESTRATOR has no general root write authority beyond those three plus `research-status.html` under this specific grant. Authoritative scientific/protocol/Git artifacts always override the dashboard's content if any discrepancy occurs.

### Operational reporting convention for `research-status.html` (non-methodological)

This is an operational convention only. It does not alter any Rule above, any protocol document, or any write boundary.

- `research-status.html` is a living, descriptive-only project-status report. It is **not** protocol, evidence, adjudication, a human ruling, or a statistical result, and it is never used as an input to scientific computation.
- Update it only **after** the authoritative artifact it describes (a commit, a manifest field, a change-control record, a result artifact) is finalized and independently validated — never before. The order is always: authoritative artifact → validation → `research-status.html` update.
- Authoritative protocol, manifests, immutable audit/adjudication artifacts, human rulings, derived scientific results, and Git provenance always override this page if any discrepancy occurs.

### E03 CI/CD infrastructure — narrow authorization (E03-CI-WRITE-AUTH-01)

Human decision, 2026-09-22: **GitHub Actions is the E03-resetability CI/CD orchestrator.** Under this specific, narrow authorization only:

- The ORCHESTRATOR role may create and maintain `.github/workflows/**` for authorized E03-resetability GitHub Actions infrastructure (configuration validation, provisioning / environment verification, mobile-qualification preparation, and gated measured execution). This is the only CI-configuration path any role may write; the write-boundary matrix of protocol/agent-governance-v2.md (FROZEN-PRE-DATA 2026-09-22, authorization id AGENT-GOVERNANCE-V2-FREEZE-AUTH-01; v1 AMENDED) is otherwise unchanged, and the grant is incorporated in that document's sections 3.5 and 4. EXECUTOR-E03-RESETABILITY may only trigger and read an already-authorized, version-pinned workflow, consume its explicitly assigned frozen inputs, and produce its authorized raw-data/artifacts; it never authors or modifies a workflow, changes GitHub repository settings, runner images, retry, concurrency, timeout, pinned runtime versions, or artifact-retention policy, merges pull requests, or approves its own changes.
- Every future E03 **MEASURED_EXPERIMENT** execution must run through the frozen GitHub Actions execution path. Local executions are permitted only as DEVELOPMENT, SYNTAX_VALIDATION, TOOL_QUALIFICATION, SMOKE, ENVIRONMENT_VERIFICATION, or DRY_RUN and never enter the measured E03 dataset. The measured workflow fails closed unless every frozen input and gate it enumerates is present (E03 campaign START, frozen configuration, CS-001 mapping FROZEN and active, CS-002 excluded, schema v2, frozen agent governance, mobile qualification for mobile cells, verified controlled instance, pinned repository commit and workflow revision, frozen executor prompt). This section does not authorize E03 START.
- The study repository intended for GitHub Actions must be **PUBLIC**; it is not made private without a new explicit human instruction.
- **Publication hygiene (binding for every commit, branch, tag, workflow, job, artifact, and public-facing file):** no AI-assistant or vendor attribution or branding, no assistant-attribution commit trailers, no assistant session or conversation identifiers, no assistant-named branches, jobs, or artifacts, no chat transcripts, scratch reasoning, or temporary agent notes. Commits use the configured human/project git identity and describe the scientific or engineering change itself. This hygiene never erases scientifically required provenance, never alters immutable evidence, and never makes an affirmative claim that no AI-assisted tooling was used. The canonical repository's history is preserved internally exactly as recorded (never rewritten); the public repository receives a provider-neutral clean reproducibility snapshot whose every difference from the canonical tree is documented in its export manifest (PUBLIC-EXPORT-MANIFEST.yaml).
- No general CI authorization exists for E02, E04–E12, repository build automation unrelated to the study, other CI platforms, or arbitrary CI usage; each would require its own separate human authorization. The lightweight-repository intent of the Style rule below is preserved everywhere else.

## Where the rules live

| Concern | Location |
|---|---|
| Study design, RQ1–RQ5, hypotheses H1–H5, modality list | protocol/study-design-v1.md |
| Capability scale (Score 0 — UNSUPPORTED to Score 3 — EXPERIMENTALLY CONTROLLABLE) | protocol/capability-rubric-v1.md |
| Cross-cutting properties | protocol/cross-cutting-properties-v1.md |
| Setup Effort recording | protocol/setup-effort-v2.md (v1 AMENDED 2026-09-21; superseded by v2, which resolves issue (xviii)) |
| Repetition targets and determinism procedures | protocol/repeatability-determinism-v1.md |
| Pre-registered analysis plan; CONFIRMATORY vs EXPLORATORY comparisons | protocol/statistical-analysis-plan-v1.md |
| Semantic equivalence of cross-ecosystem scenarios; NOT_COMPARABLE | protocol/equivalent-scenario-mapping-v1.md |
| Environment provenance (environment_type) and experimental control; interpretation rules; Open-Issue Register | protocol/study-design-v1.md sections 10, 11, 13 |
| Mobile runner policy, qualification gate (pass criteria), per-SUT compatibility smoke | protocol/mobile-runner-policy-v1.md |
| Evidence statuses, evidence record fields (evidence_id), execution verification records | protocol/evidence-rules-v1.md |
| Protocol lifecycle, versioning, change log | protocol/change-control-v1.md |
| Agent roles and write boundaries | protocol/agent-governance-v2.md (FROZEN-PRE-DATA 2026-09-22; v1 AMENDED); freeze record manifests/agent-governance-v2-freeze-v1.yaml |
| Study, SUT, and toolchain manifests | manifests/ |
| JSON schemas for manifests and records | schemas/ |
| Data and prompt handling | raw-data/README.md, derived-data/README.md, analysis/README.md, prompts/README.md |
| E03 canonical-scenario catalogue (active: CS-001 only; CS-002 excluded, preserved) | experiments/E03-resetability/canonical-scenario-catalogue.md; manifests/e03-cs002-exclusion-v1.yaml |
| Run-manifest schema versions (v1 preserved; v2 adds only `actions[].outcome`) | schemas/README.md; manifests/run-manifest-schema-v2-provenance.yaml |
| E03 CI/CD execution model, GitHub Actions workflows, publication hygiene | experiments/E03-resetability/ci-cd-execution-model.md; .github/workflows/ (E03-CI-WRITE-AUTH-01, above) |
| Public reproducibility export (mode, rules, export log) | PUBLIC-EXPORT-MANIFEST.yaml |
| Mobile qualification: frozen-input package (v2 current, v1 history); implementation lock (v2 current, v1 history); AUTH-02 record (superseded before dispatch) and its supersession record (PROTO-U12); AUTH-03 record (issued 2026-09-23, not dispatched); the retired authorization record (record_validity INVALID); the PROTO-U10 correction record (quarantine, restored runner state, exposure, hashes, verbatim human instruction); frozen launch prompt; quarantined gate records; runner selection (none) | manifests/mobile-qualification-package-v2.yaml (v1); manifests/mobile-qualification-implementation-lock-v2.yaml (v1); manifests/mobile-qualification-execution-auth-v2-prepared.yaml; manifests/mobile-qualification-auth-02-dispatch-supersession-v1.yaml; manifests/mobile-qualification-execution-auth-v3-prepared.yaml; manifests/mobile-qualification-execution-auth-v1.yaml; manifests/mobile-qualification-authorization-correction-v1.yaml; prompts/frozen/qualifier-mobile-01-mobilewright-gate-v1.md; qualification/mobilewright/README.md; qualification/mobilewright/output/QUARANTINE-RECORD.yaml; manifests/toolchain-manifest.yaml (`mobile_runner_selection`) |

## Agent roles and write boundaries

Roles: ORCHESTRATOR; AUDITOR-SUT01-OMNIPIZZA; AUDITOR-SUT02-SAUCELABS; AUDITOR-SUT03-BROWSERSTACK; AUDITOR-SUT04-RESTFULBOOKER; AUDITOR-SUT05-JUICESHOP; AUDITOR-SUT06-WEBDRIVERIO; ADJUDICATOR-EU-01; QUALIFIER-MOBILE-01; EXECUTOR-E03-RESETABILITY (defined, not launched; E03-resetability only). Each role writes only inside its assigned workspace (for example, an AUDITOR writes only under its own audits/SUT-xx-... directory); only the ORCHESTRATOR writes to protocol/ and manifests/, and any freeze or version change additionally requires explicit human approval; under E03-CI-WRITE-AUTH-01 (above), only the ORCHESTRATOR writes `.github/workflows/**`. Authoritative definitions: protocol/agent-governance-v2.md (FROZEN-PRE-DATA 2026-09-22, in force; protocol/agent-governance-v1.md AMENDED). The frozen role definition of EXECUTOR-E03-RESETABILITY authorizes no execution: launching it still requires E03 campaign START, a frozen executor prompt, and every other gate.

## Style

- Plain Markdown, YAML, CSV, and JSON. No databases, no Docker, no CI, no package managers unless required for local validation. Sole exception, narrow and human-authorized (E03-CI-WRITE-AUTH-01, above): GitHub Actions workflow files under `.github/workflows/**` for E03-resetability measured execution and its validation. Nothing else about this rule changes; no other campaign, CI platform, or build automation is covered.
- KISS / YAGNI / DRY: precise and complete, no padding, no framework abstractions. Reference sibling documents by relative repository path instead of duplicating their content.
- Unknown provenance is null (YAML/JSON) or "null / TBD" (Markdown), never a guess. Open choices are labeled "candidate — not frozen" or "TBD before freeze".
- Expand "SUT" as "system under test" on first use in each document.

## Before finishing any task

- Every YAML and JSON file you touched parses.
- No invented provenance: no commit SHA, release tag, tool version, URL, repository location, measurement, score, or result that was not actually observed and recorded.
- No language that favors OmniPizza (SUT-01) or treats it as reference, framework, or expected winner.
- "Appium 2" appears only in a statement that it must not be used; no Appium version other than Appium 3 is otherwise mentioned.
- Protocol state and version of every file under protocol/ are unchanged unless explicitly instructed.
- Every reference to an evidence record uses evidence_id; no CSV row or line number is used as a reference; every evidence.csv keeps the identical thirteen-field header.
- No global weighted Experimental Utility Score, overall winner, or ranking across dimensions was introduced; no N/A capability level was introduced.
- You wrote only inside your assigned workspace (rule 16); the only file under `.github/` you created or changed is under `.github/workflows/**`, as ORCHESTRATOR, under E03-CI-WRITE-AUTH-01.
- No AI-assistant or vendor attribution, assistant-attribution commit trailer, or session/conversation identifier was introduced into any commit message, git identity, branch, tag, workflow, job, artifact name, or public-facing file, and no scientifically required provenance was erased to achieve this (E03-CI-WRITE-AUTH-01, publication hygiene).
- No workflow, script, or record performs or labels a MEASURED_EXPERIMENT unless every frozen E03 gate is present; provisioning, environment verification, qualification, smoke, and dry-run outputs are labeled as such and never enter raw-data/ as measured data.
