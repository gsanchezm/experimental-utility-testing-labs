# prompts/ — Agent Prompts

Study: Experimental Utility of Software-Testing Laboratory Ecosystems (EUS-2026-001). Protocol v1/v2 FROZEN-PRE-DATA (protocol/change-control-v1.md). This directory holds the prompts given to the agent roles defined in protocol/agent-governance-v1.md when they act on a system under test (SUT) or on study data.

## Directories

| Directory | Content | Editable |
|---|---|---|
| prompts/working/ | Draft agent prompts under development, before use in any campaign | yes |
| prompts/frozen/ | Exact prompts actually used during data collection, versioned | no — a change creates a new version |

## Rules

1. **working/ contains draft agent prompts.** Drafts may be edited freely until the moment they are used for a campaign.
2. **frozen/ contains prompts actually used during data collection.** Once an agent prompt is used for a campaign, the exact prompt text is copied into frozen/ with a version identifier, no later than the first run that uses it.
3. **Naming convention (candidate — not frozen):** `<role>-<campaign-id>-v<N>.md`, for example `AUDITOR-SUT03-BROWSERSTACK-E01-capability-audit-v1.md`. `<role>` is a role name from protocol/agent-governance-v1.md; `<campaign-id>` is the full campaign directory name (E01-capability-audit through E12-localization-i18n), as used in raw-data/ and in run manifests; `<N>` starts at 1 and increments with every change.
4. **Never silently edit a frozen prompt.** Any change creates a new version file (`v<N+1>`) and is recorded in the change log per protocol/change-control-v1.md. The earlier version stays on disk unchanged.
5. **Frozen prompts are provenance for the runs that used them.** Every run manifest (schemas/run-manifest.schema.json) references the `prompt_version` of the frozen prompt under which the run was executed. A run whose prompt_version does not resolve to a file in frozen/ is not attributable, with one exception: a run executed by a human operator without an agent prompt records prompt_version null, the operator in agent_role, and the reason in notes, and is attributable through its run manifest.
6. Prompts for the same campaign give every SUT the same task and the same rules (AGENT-INSTRUCTIONS.md rules 10–12). Per-role differences are limited to the assigned SUT, its workspace, and the modalities applicable to it. No prompt presupposes a score, result, or ranking for any SUT.

## Tracking

Both directories are tracked by git; each contains a .gitkeep so the empty directory is committed. .gitignore re-includes prompts/frozen/** and prompts/working/** after its noise patterns.

## Current status

- Three prompts are frozen (E01-capability-audit only): `prompts/frozen/e01-capability-auditor-v1.md` (used by all six auditors); `prompts/frozen/e01-adjudicator-v1.md` (superseded in place per PROTO-U03's remedy — a non-substantive correction to its self-disqualifying status line — and immediately followed by `prompts/frozen/e01-adjudicator-v2.md`, the version actually used by ADJUDICATOR-EU-01).
- No prompt exists yet for any other campaign, including E03-resetability: prompt authoring and freeze are an explicit, later step of E03's own campaign-start sequence, not performed during PRE-START preparation (`protocol/agent-governance-v2.md`, FROZEN-PRE-DATA 2026-09-22).
- The mobile qualification gate prompt was first frozen as `prompts/frozen/qualifier-mobile-01-mobilewright-gate-v1.md` (version id `qualifier-mobile-01-mobilewright-gate-v1`; byte-identical copy of `prompts/working/qualifier-mobile-01-mobilewright-gate-v1.md`, sha256 `edbeb9f524392e0d3cfb82c5a7cff92908681ecfece37cb788f4fbf32b3c3ba8`), frozen 2026-09-22 by the ORCHESTRATOR when it relayed the text recorded as MOBILE-QUALIFICATION-EXEC-AUTH-01 (`manifests/mobile-qualification-execution-auth-v1.yaml`, which also records the three launch variables rendered for the launched copy; that record is INVALID — the text was never a human-issued authorization, PROTO-U10 — so the freeze stands as a prompt freeze but the launch it accompanied was unauthorized). It is a tool-qualification prompt, not a campaign prompt: it is used by QUALIFIER-MOBILE-01 for the Mobilewright qualification gate only (`qualification/mobilewright/`); every record that role produces cites its version id. The executions performed under v1 on 2026-09-22 are quarantined because the authorization relayed with it was never issued by a human (protocol/unresolved.md, PROTO-U10). `prompts/frozen/qualifier-mobile-01-mobilewright-gate-v1.md` is retained unchanged, including its stale "Status: WORKING PROMPT, not frozen" header line, as **historical provenance for the AUTH-01 lineage only** — it is not silently corrected in place, and it is no longer the active launch prompt for any authorization.
- **v2 (2026-09-23) is the exact rendered frozen launch prompt for MOBILE-QUALIFICATION-EXEC-AUTH-02.** `prompts/frozen/qualifier-mobile-01-mobilewright-gate-v2.md` (version id `qualifier-mobile-01-mobilewright-gate-v2`, sha256 `6d2414c5c685755addb4e966f1858171bdbf927df6998a0c27a153d83a639a00`; byte-identical copy of `prompts/working/qualifier-mobile-01-mobilewright-gate-v2.md`), change-log row per rule 4 above, precedent identical to the e01-adjudicator v1→v2 correction (PROTO-U03). This corrects an earlier representation defect: when AUTH-02 was issued (2026-09-23), the ORCHESTRATOR initially reasoned that the sentence formerly at the end of this bullet — "the frozen prompt itself is unchanged and remains the launch prompt for a future gate under a new authorization id" — meant v1's unrendered `{{INSTRUCTION_ID}}`-style template could simply be reused, with the rendering recorded only as data in the authorization record. That reasoning is not being retracted as a **general** rule (a still-unchanged template genuinely can be discovered and reused by id); it was insufficient for AUTH-02 specifically because the human's own issuing instruction explicitly required a frozen prompt that "actually contains AUTH-02" as literal text. v2 differs from v1 in exactly two permitted ways — an accurate frozen-status header (naming the authorization and prompt version) and the three launch variables rendered as literal text, including their one inline occurrence in step 1 — verified line-by-line (6 of 44 lines differ, all accounted for); no methodological or procedural change. See protocol/CHANGELOG.md, 2026-09-23 row, for the full correction record.
- **v3 (2026-09-23) is the frozen launch prompt of MOBILE-QUALIFICATION-EXEC-AUTH-03** (`prompts/frozen/qualifier-mobile-01-mobilewright-gate-v3.md`, version id `qualifier-mobile-01-mobilewright-gate-v3`, canonical sha256 `e7322c0a60e0592924fa1fac41526b862640f8e9c6925540e4b057e1d6dfe273`), frozen at AUTH-03's explicit human issuance on 2026-09-23 as a byte-for-byte copy of the reviewed working copy `prompts/working/qualifier-mobile-01-mobilewright-gate-v3.md` (sha256 `961e6ab233e0022a7f7b3d5e7a43735a507ec54f7a3c358a9f9dab7b4f7c4320`, retained unchanged as historical preparation provenance) with only its two `{{INSTRUCTION_DATE}}` placeholders rendered to `2026-09-23` (lines 8 and 27; no other line differs); AUTH-03 was ISSUED (VALID, dispatch-ELIGIBLE; `manifests/mobile-qualification-execution-auth-v3-prepared.yaml`) and then dispatched exactly once on a separate explicit human instruction (run 35938250936, attempt 1, 2026-09-24; `manifests/mobile-qualification-auth-03-dispatch-v1.yaml`); its dispatch lifecycle was then closed in place (`dispatch_eligibility: CONSUMED_AFTER_DISPATCH`, 2026-09-24; still VALID / ISSUED); its 66 execution records and its import record carry this prompt version id, and the gate result (FAIL; Appium 3 selected by the frozen switch rule) is recorded in `qualification/mobilewright/README.md`. The remainder of this bullet describes the preparation state. MOBILE-QUALIFICATION-EXEC-AUTH-02 was superseded before dispatch (PROTO-U12, pre-dispatch execution-integrity / provenance defects; never dispatched), so v2 is not reused for a new authorization id; v2 stays frozen and unchanged as the prompt of the superseded AUTH-02 lineage. v3 differs from v2 only in isolation / provenance / representation (formal output namespace `qualification/mobilewright/formal/<authorization id>/`, gate → Android → iOS in one dispatch, implementation-lock v2 and package v2 as inputs, the quarantine named as a non-input, rendered launch variables; protocol/CHANGELOG.md, 2026-09-23 row). It is frozen to `prompts/frozen/` only at the human issuance of AUTH-03, with only its `INSTRUCTION_DATE` placeholder rendered; preparing or freezing it is not issuance and not dispatch authorization.
