# manifests/mobile-qualification-execution-auth-v2-prepared.yaml
# STATUS: ISSUED (promoted in place 2026-09-23; see issuance block below). The "-prepared" filename is now HISTORICAL /
# PROVENANCE-ORIENTED ONLY -- it names the file that carried this authorization_id from its PREPARED state through
# issuance, per the promotion procedure this file itself froze (AUTH-02 PRE-ISSUANCE CONSISTENCY HARDENING instruction,
# section 7: "promote this SAME prepared file in place"; explicit human instruction, 2026-09-23, section "AUTH-02
# PROMOTION": "if the filename retains the -prepared suffix, explicitly document that the filename is historical /
# provenance-oriented and that the record's current lifecycle fields are authoritative"). The lifecycle fields below
# (status, record_validity, issued_by, issued_at, authorizes_execution) are authoritative; the filename is not renamed,
# per the same instruction ("do not create a duplicate authorization file merely to remove the suffix") and per the
# duplicate-record prohibition already in force (no second file may ever carry this authorization_id).
#
# History preserved (this file was NOT an authorization from 2026-09-22 to 2026-09-23; git history shows this verbatim):
# it began as a PREPARED DRAFT of the next MOBILE_QUALIFICATION_EXECUTION authorization package, assembled by the
# ORCHESTRATOR under the LOCAL PREFLIGHT CLOSURE instruction, section 12 ("prepare, but do NOT execute... do not mark it
# issued yet"), specifically to guard against the PROTO-U10 failure mode (manifests/mobile-qualification-authorization-
# correction-v1.yaml): a relayed/drafted authorization text later treated as if a human had issued it. Guards that were
# in force while PREPARED, and the reasoning that applies to any future authorization file of this kind, are preserved
# below unedited in substance:
#   - while PREPARED, status was PREPARED_AWAITING_EXPLICIT_HUMAN_AUTHORIZATION, issued_by was null, authorizes_execution
#     was false, and there was no issued_at date -- exactly the state a proposed/drafted/relayed text must show, and the
#     exact state the readiness gate (hardened 2026-09-23) refuses to dispatch.
#   - a proposed/drafted/relayed authorization text is never itself an authorization. Nor is a file's mere presence in
#     the repository, nor its discoverability by the workflow. An authorization exists only when the human explicitly
#     replies with language issuing/approving it -- which is exactly what happened on 2026-09-23 (see below); nothing
#     before that reply changed this file's authorization state.
#   - issuance NEVER creates a second authorization record. MOBILE-QUALIFICATION-EXEC-AUTH-02 was promoted in place, in
#     THIS file; no second file carrying this authorization_id exists or may ever be created (it would violate the
#     gate's "exactly one matching record" requirement).
#
# ISSUANCE (2026-09-23): the human research lead explicitly issued and approved MOBILE-QUALIFICATION-EXEC-AUTH-02 as
# live session input to the ORCHESTRATOR ("I explicitly issue and approve: MOBILE-QUALIFICATION-EXEC-AUTH-02 ... Issued
# by: gilbertosanchez"), restating the unchanged scope (runner, SUT, platforms, scenarios, N, criteria, attribution,
# fallback, implementation lock, scenario realization) and explicitly excluding local-preflight/quarantined/DEVELOPMENT
# observations as formal input, and explicitly withholding dispatch authority in the same message ("DO NOT DISPATCH THE
# 60 QUALIFICATION RUNS YET"). This is a genuine issuance, not a proposal or draft: distinguished from PROTO-U10 by
# arriving as the human's own first-person statement in this session, exactly the class of event the guards above (and
# the ORCHESTRATOR's standing practice since PROTO-U10) require before promoting any record from PREPARED to ISSUED.
#
# PRE-DISPATCH SUPERSESSION (2026-09-23, PROTO-U12; manifests/mobile-qualification-auth-02-dispatch-supersession-v1.yaml):
# before any dispatch, the ORCHESTRATOR's pre-dispatch check found three execution-integrity / provenance defects in the
# implementation and package this authorization was issued against (formal output into the quarantined
# qualification/mobilewright/output/; cross-platform parallelism against the shared qualification account; active prompt
# version hardcoded as v1). By explicit human instruction ("MOBILE QUALIFICATION PRE-DISPATCH INTEGRITY CORRECTION",
# 2026-09-23) this authorization must no longer be used for dispatch. The historical facts are preserved, not rewritten:
# it was genuinely issued by the human research lead on 2026-09-23 (authorization_provenance: VALID; status, issued_by,
# issued_at unchanged) and it was NEVER dispatched. Only its current dispatch-eligibility fields change (record_validity,
# authorizes_execution, dispatch_authorized_by_this_record, dispatch_eligibility; each previous value is kept in a
# comment). The readiness gate additionally refuses this id explicitly. The valid lineage continues with
# MOBILE-QUALIFICATION-EXEC-AUTH-03 (manifests/mobile-qualification-execution-auth-v3-prepared.yaml; PREPARED, NOT ISSUED).
record_class: MOBILE_QUALIFICATION_EXECUTION_AUTHORIZATION
status: ISSUED
qualification_evidence: false
measured_experiment: false
authorizes_execution: false  # PROTO-U12, 2026-09-23: was true from issuance (2026-09-23) until supersession before dispatch; nothing was ever executed under it
selects_runner: false
record_validity: SUPERSEDED  # PROTO-U12, 2026-09-23: was VALID; superseded before dispatch (exact lifecycle term refused by the readiness gate). Not INVALID: unlike AUTH-01 (PROTO-U10), the human issuance is genuine -- see authorization_provenance
authorization_provenance: VALID  # PROTO-U12: the human issuance of 2026-09-23 is genuine and remains valid provenance
dispatch_eligibility: SUPERSEDED  # PROTO-U12: superseded / retired BEFORE EXECUTION; never dispatched; the readiness gate admits only dispatch_eligibility: ELIGIBLE and refuses this id explicitly
dispatch_eligibility_record: manifests/mobile-qualification-auth-02-dispatch-supersession-v1.yaml
study_id: EUS-2026-001
prepared_by: ORCHESTRATOR
prepared_at: "2026-09-22"
prepared_under_instruction: "LOCAL PREFLIGHT CLOSURE instruction (* PREPARATION FOR VALID CI QUALIFICATION), section 12, entered directly by the human operator as live session input, 2026-09-22"
authorization_id: MOBILE-QUALIFICATION-EXEC-AUTH-02  # a NEW id; MOBILE-QUALIFICATION-EXEC-AUTH-01 is retired (PROTO-U10) and must never be reused
authorization_type: MOBILE_QUALIFICATION_EXECUTION
issued_by: gilbertosanchez  # explicit human issuance, live session input, 2026-09-23 -- see the ISSUANCE note above
issued_at: "2026-09-23"
issued_under_instruction: "EUS-2026-001 — MOBILE QUALIFICATION issuance instruction (\"I explicitly issue and approve: MOBILE-QUALIFICATION-EXEC-AUTH-02 ... Issued by: gilbertosanchez\"), entered directly by the human operator as live session input, 2026-09-23; explicitly withholds dispatch authority in the same message (section \"IMPORTANT EXECUTION BOUNDARY\": do not dispatch the 60 qualification runs, DEVELOPMENT mode, Appium, compatibility smoke, runner selection, or E03 in this operation)"
dispatch_authorized_by_this_record: false  # PROTO-U12, 2026-09-23: was true (the record satisfied the readiness gate from issuance until supersession); superseded before dispatch
dispatch_performed_under_this_operation: false  # the issuance instruction explicitly withheld dispatch; no workflow was run

launch_prompt:  # CORRECTED 2026-09-23 (AUTH-02 FROZEN PROMPT PROVENANCE CORRECTION instruction). SUPERSEDED representation,
                # preserved for history only (do not act on it): this block previously pointed at the unchanged, still-
                # parametrized v1 template and recorded the rendering only as data here, reasoning that prompts/README.md's
                # forward-looking note ("the frozen prompt itself is unchanged and remains the launch prompt for a future
                # gate under a new authorization id") made that sufficient. The human explicit issuance instruction had
                # asked for something more specific -- "a NEW QUALIFIER-MOBILE-01 launch prompt" that "actually contains
                # AUTH-02" as literal text -- which the reused-template representation did not satisfy literally. Corrected:
                # a new frozen file, prompts/frozen/qualifier-mobile-01-mobilewright-gate-v2.md, contains the exact launch
                # text (v1's substantive procedure, byte-for-byte, plus the three launch variables actually rendered and an
                # accurate frozen-status header); verified below to differ from v1 in only those permitted ways (no
                # methodological or procedural change; qualification/mobilewright/README.md, "prompt normalization" note).
  frozen_copy: prompts/frozen/qualifier-mobile-01-mobilewright-gate-v2.md
  prompt_version_id: qualifier-mobile-01-mobilewright-gate-v2
  sha256: 6d2414c5c685755addb4e966f1858171bdbf927df6998a0c27a153d83a639a00
  frozen_at: "2026-09-23"
  derived_from: "prompts/frozen/qualifier-mobile-01-mobilewright-gate-v1.md (sha256 edbeb9f524392e0d3cfb82c5a7cff92908681ecfece37cb788f4fbf32b3c3ba8, preserved byte-for-byte unchanged as historical provenance for the AUTH-01 lineage, including its stale 'Status: WORKING PROMPT, not frozen' header text, which is not silently corrected in place)"
  rendered_variables:  # now rendered as literal text IN the frozen v2 file itself, not only recorded here as data
    INSTRUCTION_ID: MOBILE-QUALIFICATION-EXEC-AUTH-02
    INSTRUCTION_DATE: "2026-09-23"
    PROMPT_VERSION_ID: qualifier-mobile-01-mobilewright-gate-v2
  verification: "prompts/frozen/qualifier-mobile-01-mobilewright-gate-v2.md contains the literal string MOBILE-QUALIFICATION-EXEC-AUTH-02 (twice: the rendered-variables list and step 1 of the procedure) and does not contain MOBILE-QUALIFICATION-EXEC-AUTH-01 or any unresolved {{...}} placeholder anywhere. Diffed line-by-line against v1 (44 lines each): exactly 6 lines differ (the title, the status/header paragraph, the three variable-definition lines, and step 1's inline instantiation of the same three variables); applying only the three variable substitutions to v1's step-1 line reproduces v2's step-1 line exactly (byte-for-byte). No other line of role, boundaries, frozen inputs, procedure steps 2-10, or 'what you never do' changed."
  execution_view_provenance: PUBLIC-EXPORT-MANIFEST.yaml  # CANONICAL vs PUBLIC EXECUTION PROMPT HASH correction, 2026-09-23. This field is
    # a pointer only, not a duplicated hash: sha256 above is this record's own canonical provenance claim (the exact
    # canonical frozen artifact, verified against the working tree at authorization time); the public execution
    # checkout that GitHub Actions actually runs from is a declared, deterministic PROVIDER_NEUTRAL_PATH_ALIAS
    # transformation of that same artifact (this file's "Binding rules: AGENT-INSTRUCTIONS.md" line becomes "Binding rules:
    # AGENT-INSTRUCTIONS.md" on export; nothing else differs -- see prompts/README.md's v1->v2 diff note for the
    # full six-line accounting, which the transformation does not add to). The public export's own
    # PUBLIC-EXPORT-MANIFEST.yaml is the authoritative bridge between the two: it records, per exported file, both
    # canonical_sha256 (must equal sha256 above) and exported_sha256 (the exact bytes present in that checkout),
    # plus the declared transformation class. AUTH-02 does not duplicate exported_sha256 here -- that value is
    # export-run bookkeeping, not authorization provenance, and belongs solely in the manifest the export itself
    # produces. The readiness gate (.github/workflows/e03-mobile-qualification.yml) verifies the full chain --
    # AUTH-02.sha256 == manifest.canonical_sha256, and SHA256(on-disk prompt) == manifest.exported_sha256, with the
    # transformation class restricted to a short allowed list -- rather than ever comparing AUTH-02.sha256 directly
    # against on-disk bytes in the (always-transformed) public checkout.
  prompt_provenance_record:  # descriptive summary of the two legitimate representations; neither supersedes the other
    canonical_prompt: "prompts/frozen/qualifier-mobile-01-mobilewright-gate-v2.md (the frozen canonical source, authored and hashed on the canonical working tree)"
    canonical_sha256: 6d2414c5c685755addb4e966f1858171bdbf927df6998a0c27a153d83a639a00
    public_execution_view: "the same file as exported into the public repository's checkout, after the declared provider-neutral transformation; this is the exact byte sequence any real GitHub Actions dispatch would read, since that is the only repository the workflow can run from"
    public_execution_sha256: "derived from the current PUBLIC-EXPORT-MANIFEST.yaml exported_sha256 entry for this path, not hardcoded here; verify against the currently published export, not a value frozen into this record"
    transformation: PROVIDER_NEUTRAL_PATH_ALIAS
    semantic_change: "NONE -- one string substitution (AGENT-INSTRUCTIONS.md -> AGENT-INSTRUCTIONS.md) inside the role/boundaries paragraph's 'Binding rules' citation; no role, procedure, scenario, criterion, constraint, or launch-variable rendering differs between the two byte sequences"
    note: "the two byte sequences are NOT identical and this record does not claim they are; they are two legitimate, independently verifiable representations of the same authorized artifact, connected by a declared, auditable, scientifically-inert transformation -- not by assertion"

provenance_rule:  # restated verbatim, still in force (originally AUTH-02 PRE-ISSUANCE CONSISTENCY HARDENING instruction, section 8; unaffected by issuance -- these are permanent, not conditional on PREPARED status)
  - "THE PREPARED FILE IS NOT AUTHORIZATION." # historical: true of this file from 2026-09-22 to 2026-09-23, before issuance
  - "THE PRESENCE OF AUTH-02 IN THE PUBLIC REPOSITORY IS NOT AUTHORIZATION." # still true in general; this record's own ISSUED status is what carries authority now, not its publication
  - "DISCOVERY BY THE WORKFLOW IS NOT AUTHORIZATION." # still true in general; the record's fields, not discoverability, carry authority
  - "ONLY AN EXPLICIT HUMAN ISSUANCE MAY CHANGE AUTH-02 FROM PREPARED TO ISSUED." # what happened on 2026-09-23, and the standing rule for any future authorization record

references:
  governing_policy: protocol/mobile-runner-policy-v1.md  # FROZEN-PRE-DATA; pass criteria section 7, switch rule section 8
  governance_in_force: protocol/agent-governance-v2.md  # FROZEN-PRE-DATA 2026-09-22; QUALIFIER-MOBILE-01 writes qualification/** only
  frozen_input_package: manifests/mobile-qualification-package-v1.yaml  # unchanged; its pins remain the gate's inputs, re-verified below against this file, not copied from the closure instruction's own restatement
  implementation_baseline_lock: manifests/mobile-qualification-implementation-lock-v1.yaml  # MOBILE-QUALIFICATION-IMPLEMENTATION-LOCK-01; eleven artifacts, reverified byte-identical as recently as the iOS local-preflight retry of 2026-09-22
  proto_u10_correction: manifests/mobile-qualification-authorization-correction-v1.yaml  # the record documenting why AUTH-01 is retired and invalid; this draft exists specifically because of it
  locked_qualification_harness: [qualification/mobilewright/run-gate-android.sh, qualification/mobilewright/run-gate-ios.sh, qualification/mobilewright/gate-common.sh, "qualification/mobilewright/harness/**"]
  existing_workflow: .github/workflows/e03-mobile-qualification.yml  # readiness gate corrected 2026-09-23 (CI_AUTHORIZATION_RESOLUTION_DEFECT): it globs manifests/mobile-qualification-execution-auth-v*.yaml and DOES discover this file by its unique authorization_id; discovery still fails closed today because this record's status/issued_by/authorizes_execution/record_validity/frozen-prompt fields do not (yet) show an issued authorization -- see the header guards above
  frozen_launch_prompt_precedent: prompts/frozen/qualifier-mobile-01-mobilewright-gate-v1.md  # the AUTH-01 prompt freeze; its rendered INSTRUCTION_ID is AUTH-01 and cannot be reused verbatim -- a NEW prompt freeze rendering INSTRUCTION_ID = MOBILE-QUALIFICATION-EXEC-AUTH-02 is a step to perform at actual issuance, not created by this draft

authorized_scope_if_issued:  # copied from, and cross-checked against, manifests/mobile-qualification-package-v1.yaml and protocol/mobile-runner-policy-v1.md at draft time; not copied from the closure instruction's own restatement of these numbers, which is a consistency check, not a source
  runner_under_qualification: "Mobilewright 0.0.60 (primary candidate; npm mobilewright@0.0.60, source tag v0.0.60, commit 2046d2d95e7db6383183bb6b116f81b1236cae42)"
  fallback_on_switch_rule: "Appium 3, pinned 3.7.0 (protocol/mobile-runner-policy-v1.md, section 8); Appium 2 never"
  qualification_sut: "OmniPizza (SUT-01), pinned qualification build release v1.1.8 (tag commit 3cdbe6596a1ab5ef2058ba4259c44da96a0fa03a; omnipizza-release.apk sha256 1059e946...6a4; OmniPizza-Simulator.zip sha256 de2e8c21...eb1)"
  formal_platforms:
    android: "pinned GitHub-hosted ubuntu-24.04 emulator substrate (ReactiveCircus/android-emulator-runner v2.38.0 pinned at a421e43855164a8197daf9d8d40fe71c6996bb0d); NOT the Samsung Galaxy Z Flip 6 physical device used in the local preflight"
    ios: "pinned GitHub-hosted macos-15 runner (image 20260824.0482.1 or later shipping Xcode 16.4 build 16F6 and iOS 18.5); NOT the operator's local Xcode Simulator used in the local preflight"
  scenarios: [MQ1, MQ2, MQ3]
  mandatory_capabilities: "MC-01 through MC-10 (protocol/mobile-runner-policy-v1.md, section 5), each exercised on both platforms"
  measured_executions_per_combination: 10  # N = 10, policy section 7.1
  combinations: 6  # MQ1/MQ2/MQ3 x android/ios
  total_measured_executions: 60
  pass_criteria: "protocol/mobile-runner-policy-v1.md, section 7.2, unchanged: (1) 100% MC-01..MC-10 support both platforms; (2) 10/10 successful measured executions per combination; (3) no repeated runner-caused blocker of the same mandatory capability across two or more executions; (4) no manual intervention inside any measured execution"
  attribution_and_exclusion_rules: "policy section 7.3, unchanged: infrastructure/SUT_INSTANCE/API_SEED/OPERATOR/OTHER failures excluded with a complete attribution record and replaced; uncertain attribution is runner-caused; runner-caused failures never excluded"
  fallback_rule: "policy section 8, unchanged: on failure of a mandatory capability or violation of criterion 7.2.3/7.2.4, switch to Appium 3 (3.7.0); recorded in qualification/mobilewright/README.md, manifests/toolchain-manifest.yaml, and protocol/CHANGELOG.md; compatibility smoke repeated; Appium 2 never"
  prequalification_outcome_exposure: true  # the frozen inputs above are immutable in response to the already-observed outcomes (quarantined Android emulator MQ3/MC-07 failure; local Z Flip 6 MQ3 failure); none of them was, or may be, adjusted because of what was observed

explicitly_excluded_as_formal_input:  # LOCAL PREFLIGHT CLOSURE instruction, section 14: these may remain documented as historical/prequalification context only and must not be imported or counted as formal gate observations
  note: "formal qualification under AUTH-02, once issued, starts a new execution lineage; it does not inherit or average with any of the items below"
  items:
    - "Samsung Galaxy Z Flip 6 local diagnostic results (qualification/mobilewright/local-preflight/android-zflip6/)"
    - "local iOS Simulator local diagnostic attempts (qualification/mobilewright/local-preflight/ios-simulator/, ios-simulator-retry/)"
    - "local preflight durations"
    - "the quarantined HISTORICAL_UNAUTHORIZED_EXECUTION qualification-labelled runs (qualification/mobilewright/output/, QUARANTINE-RECORD.yaml)"
    - "any other development/warm-up run outside a formally authorized and dispatched gate under this authorization_id"

issuance_and_dispatch_status:  # supersedes the old "if_issued_next_steps" (that section described a hypothetical; this
                                # describes what has actually happened, per the same frozen procedure)
  gate_field_requirements: "the workflow's readiness gate (corrected 2026-09-23, CI_AUTHORIZATION_RESOLUTION_DEFECT; hardened 2026-09-23, AUTH-02 PRE-ISSUANCE CONSISTENCY HARDENING) discovers authorization records dynamically across manifests/mobile-qualification-execution-auth-v*.yaml and requires, on the uniquely id-matching record: authorization_type == MOBILE_QUALIFICATION_EXECUTION; record_validity explicitly present and not INVALID/SUPERSEDED/RETIRED (VALID); status == ISSUED exactly; issued_by a real human identity, not empty/placeholder/agent-role; authorizes_execution exactly true; a frozen launch prompt that exists on disk. This record now satisfies every one of these; verified statically against the actual shipped gate code (extracted, not transcribed), not dispatched -- see manifests/mobile-qualification-package-v1.yaml's sibling verification note and the ORCHESTRATOR's return package of 2026-09-23."
  duplicate_record_prohibition: "still in force, permanently, not just pre-issuance: no second file may ever carry authorization_id: MOBILE-QUALIFICATION-EXEC-AUTH-02. The gate requires exactly one matching record; a second file with the same id would make that requirement fail (0 or 2+ matches is always closed). This file (manifests/mobile-qualification-execution-auth-v2-prepared.yaml) is the one and only record of this authorization_id, promoted in place; its filename is historical and is not changed merely to remove the -prepared suffix."
  steps_completed:
    - "1. DONE 2026-09-23: the human explicitly issued MOBILE-QUALIFICATION-EXEC-AUTH-02 (live session input, not inferred from preparation, discussion, or a request for recommendations)"
    - "2. DONE 2026-09-23: ORCHESTRATOR promoted THIS SAME file in place (no second file created)"
    - "3. DONE: record_validity: VALID; status: ISSUED; issued_by: gilbertosanchez; issued_at: 2026-09-23; authorizes_execution: true (see the top of this file)"
    - "4. RESOLVED DIFFERENTLY FROM THE ORIGINAL PLAN: no new frozen prompt FILE was rendered. prompts/README.md's own note (\"the frozen prompt itself is unchanged and remains the launch prompt for a future gate under a new authorization id\") applies: the existing prompts/frozen/qualifier-mobile-01-mobilewright-gate-v1.md is a parametrized template (its only occurrences of {{INSTRUCTION_ID}} etc. are the placeholder tokens themselves, unchanged since its 2026-09-22 freeze, sha256 reverified identical); this authorization's rendering of those placeholders is recorded as data in this file's launch_prompt block, not as a second frozen file, so as not to create an unnecessary v2 prompt version for an unchanged template (prompts/README.md rule 4 reserves new versions for prompt CHANGES)"
    - "5. DONE: launch_prompt.frozen_copy, launch_prompt.prompt_version_id, launch_prompt.sha256, and launch_prompt.rendered_variables recorded above"
    - "6. DONE: qualification/mobilewright/README.md now records MOBILE-QUALIFICATION-EXEC-AUTH-02 and qualifier-mobile-01-mobilewright-gate-v1 as the new valid authorization lineage, in a section clearly separated from the HISTORICAL_UNAUTHORIZED_EXECUTION (PROTO-U10) material, which is unedited"
    - "7. DONE: the resulting clean provider-neutral export was built, scanned, and published"
  step_not_performed:
    - "8. NOT PERFORMED under this authorization's issuance operation: the GitHub Actions workflow (.github/workflows/e03-mobile-qualification.yml) was NOT dispatched. The issuing instruction explicitly withheld dispatch (\"DO NOT DISPATCH THE 60 QUALIFICATION RUNS YET\" and the same for DEVELOPMENT mode, Appium, compatibility smoke, runner selection, and E03). This record now makes dispatch POSSIBLE (AUTH_02_DISPATCHABLE: TRUE, verified statically); it does not make dispatch AUTOMATIC. Dispatch requires its own separate explicit human instruction."

human_confirmation_requested: "HISTORICAL (superseded before dispatch, PROTO-U12; no dispatch may be requested under this id): AUTH-02 is now ISSUED and dispatchable. Please reply explicitly if and when you wish the GitHub Actions qualification workflow actually dispatched (the 60 measured executions); nothing in the issuance itself triggers that."
