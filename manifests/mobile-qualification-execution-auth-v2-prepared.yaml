# manifests/mobile-qualification-execution-auth-v2-prepared.yaml
# THIS FILE IS NOT AN AUTHORIZATION. It is a PREPARED DRAFT of the next MOBILE_QUALIFICATION_EXECUTION authorization package,
# assembled by the ORCHESTRATOR under the LOCAL PREFLIGHT CLOSURE instruction, section 12 ("prepare, but do NOT execute,
# the next valid formal qualification package... do NOT mark it issued yet"). It exists to save the human the work of
# re-deriving the inputs; it grants nothing on its own. The PROTO-U10 incident (manifests/mobile-qualification-authorization-
# correction-v1.yaml) is the exact failure mode this file guards against: a relayed/drafted authorization text was later
# treated as if a human had issued it. That must not happen again. Guards (updated 2026-09-23, AUTH-02
# PRE-ISSUANCE CONSISTENCY HARDENING, after CI_AUTHORIZATION_RESOLUTION_DEFECT made the gate's discovery dynamic):
#   - status is PREPARED_AWAITING_EXPLICIT_HUMAN_AUTHORIZATION, issued_by is null, authorizes_execution is false, and there
#     is no issued_at date -- there will not be one until the human actually issues this.
#   - the workflow's readiness gate (.github/workflows/e03-mobile-qualification.yml) DOES discover this file: it globs the
#     manifests/mobile-qualification-execution-auth-v*.yaml family and this file's authorization_id
#     (MOBILE-QUALIFICATION-EXEC-AUTH-02) is unique among them, so it IS the single matching record whenever that id is
#     submitted as the dispatch instruction_id. Discoverability is not authorization: the gate still fails closed on this
#     exact record today, because status is PREPARED_AWAITING_EXPLICIT_HUMAN_AUTHORIZATION (the gate requires status ==
#     ISSUED exactly, absence or any other value fails), issued_by is null, authorizes_execution is false, record_validity
#     is absent (the gate requires it explicitly present and VALID), and no AUTH-02 frozen launch prompt exists yet.
#   - a proposed/drafted/relayed authorization text -- including this file -- is never itself an authorization. Nor is the
#     file's mere presence in the repository, nor its discoverability by the workflow. An authorization exists only when
#     the human explicitly replies with language issuing/approving it. The ORCHESTRATOR must not infer approval from an
#     advisor-generated template, from the existence of this preparation, from the workflow's ability to find this file,
#     from discussion of a possible next step, or from a request for recommendations (LOCAL PREFLIGHT CLOSURE instruction,
#     section 16; AUTH-02 PRE-ISSUANCE CONSISTENCY HARDENING instruction, section 8).
#   - issuance NEVER creates a second authorization record. MOBILE-QUALIFICATION-EXEC-AUTH-02 is promoted in place, in
#     THIS file, by editing it when issuance actually occurs (section "if_issued_next_steps" below). A second file
#     carrying the same authorization_id would violate the gate's own "exactly one matching record" requirement and is
#     explicitly prohibited (AUTH-02 PRE-ISSUANCE CONSISTENCY HARDENING instruction, section 3). Git history already
#     preserves the PREPARED state, so promotion in place does not destroy provenance.
record_class: MOBILE_QUALIFICATION_EXECUTION_AUTHORIZATION_DRAFT
status: PREPARED_AWAITING_EXPLICIT_HUMAN_AUTHORIZATION
qualification_evidence: false
measured_experiment: false
authorizes_execution: false
selects_runner: false
study_id: EUS-2026-001
prepared_by: ORCHESTRATOR
prepared_at: "2026-09-22"
prepared_under_instruction: "LOCAL PREFLIGHT CLOSURE instruction (* PREPARATION FOR VALID CI QUALIFICATION), section 12, entered directly by the human operator as live session input, 2026-09-22"
authorization_id: MOBILE-QUALIFICATION-EXEC-AUTH-02  # a NEW id; MOBILE-QUALIFICATION-EXEC-AUTH-01 is retired (PROTO-U10) and must never be reused
authorization_type: MOBILE_QUALIFICATION_EXECUTION
issued_by: null  # not issued; set only by explicit human reply issuing this authorization, never inferred
issued_at: null  # not issued; no date until issuance

provenance_rule:  # restated verbatim in force (AUTH-02 PRE-ISSUANCE CONSISTENCY HARDENING instruction, section 8)
  - "THE PREPARED FILE IS NOT AUTHORIZATION."
  - "THE PRESENCE OF AUTH-02 IN THE PUBLIC REPOSITORY IS NOT AUTHORIZATION."
  - "DISCOVERY BY THE WORKFLOW IS NOT AUTHORIZATION."
  - "ONLY AN EXPLICIT HUMAN ISSUANCE MAY CHANGE AUTH-02 FROM PREPARED TO ISSUED."

references:
  governing_policy: protocol/mobile-runner-policy-v1.md  # FROZEN-PRE-DATA; pass criteria section 7, switch rule section 8
  governance_in_force: protocol/agent-governance-v2.md  # FROZEN-PRE-DATA 2026-09-22; QUALIFIER-MOBILE-01 writes qualification/** only
  frozen_input_package: manifests/mobile-qualification-package-v1.yaml  # unchanged; its pins remain the gate's inputs, re-verified below against this file, not copied from the closure instruction's own restatement
  implementation_baseline_lock: manifests/mobile-qualification-implementation-lock-v1.yaml  # MOBILE-QUALIFICATION-IMPLEMENTATION-LOCK-01; eleven artifacts, reverified byte-identical as recently as the iOS local-preflight retry of 2026-09-22
  proto_u10_correction: manifests/mobile-qualification-authorization-correction-v1.yaml  # the record documenting why AUTH-01 is retired and invalid; this draft exists specifically because of it
  locked_qualification_harness: [qualification/mobilewright/run-gate-android.sh, qualification/mobilewright/run-gate-ios.sh, qualification/mobilewright/gate-common.sh, "qualification/mobilewright/harness/**"]
  existing_workflow: .github/workflows/e03-mobile-qualification.yml  # readiness gate corrected 2026-09-23 (CI_AUTHORIZATION_RESOLUTION_DEFECT): it globs manifests/mobile-qualification-execution-auth-v*.yaml and DOES discover this file by its unique authorization_id; discovery still fails closed today because this record's status/issued_by/authorizes_execution/record_validity/frozen-prompt fields do not (yet) show an issued authorization -- see the header guards above
  frozen_launch_prompt_precedent: prompts/frozen/qualifier-mobile-01-mobilewright-gate-v1.md  # the AUTH-01 prompt freeze; its rendered INSTRUCTION_ID is AUTH-01 and cannot be reused verbatim -- a NEW prompt freeze rendering INSTRUCTION_ID = MOBILE-QUALIFICATION-EXEC-AUTH-02 is a step to perform at actual issuance, not created by this draft

authorized_scope_if_issued:  # copied from, and cross-checked against, manifests/mobile-qualification-package-v1.yaml and protocol/mobile-runner-policy-v1.md at draft time; not copied from the closure instruction's own restatement of these numbers, which is a consistency check, not a source
  runner_under_qualification: "Mobilewright 0.0.60 (primary candidate; npm mobilewright@0.0.60, source tag v0.0.60, commit 2046d2d95e7db6383183bb6b116f81b1236cae42)"
  fallback_on_switch_rule: "Appium 3, pinned 3.7.0 (protocol/mobile-runner-policy-v1.md, section 8); Appium 2 never"
  qualification_sut: "OmniPizza (SUT-01), pinned qualification build release v1.1.8 (tag commit 3cdbe6596a1ab5ef2058ba4259c44da96a0fa03a; omnipizza-release.apk sha256 1059e946...6a4; OmniPizza-Simulator.zip sha256 de2e8c21...eb1)"
  formal_platforms:
    android: "pinned GitHub-hosted ubuntu-24.04 emulator substrate (ReactiveCircus/android-emulator-runner v2.38.0 pinned at a421e43855164a8197daf9d8d40fe71c6996bb0d); NOT the Samsung Galaxy Z Flip 6 physical device used in the local preflight"
    ios: "pinned GitHub-hosted macos-15 runner (image 20260824.0482.1 or later shipping Xcode 16.4 build 16F6 and iOS 18.5); NOT the operator's local Xcode Simulator used in the local preflight"
  scenarios: [MQ1, MQ2, MQ3]
  mandatory_capabilities: "MC-01 through MC-10 (protocol/mobile-runner-policy-v1.md, section 5), each exercised on both platforms"
  measured_executions_per_combination: 10  # N = 10, policy section 7.1
  combinations: 6  # MQ1/MQ2/MQ3 x android/ios
  total_measured_executions: 60
  pass_criteria: "protocol/mobile-runner-policy-v1.md, section 7.2, unchanged: (1) 100% MC-01..MC-10 support both platforms; (2) 10/10 successful measured executions per combination; (3) no repeated runner-caused blocker of the same mandatory capability across two or more executions; (4) no manual intervention inside any measured execution"
  attribution_and_exclusion_rules: "policy section 7.3, unchanged: infrastructure/SUT_INSTANCE/API_SEED/OPERATOR/OTHER failures excluded with a complete attribution record and replaced; uncertain attribution is runner-caused; runner-caused failures never excluded"
  fallback_rule: "policy section 8, unchanged: on failure of a mandatory capability or violation of criterion 7.2.3/7.2.4, switch to Appium 3 (3.7.0); recorded in qualification/mobilewright/README.md, manifests/toolchain-manifest.yaml, and protocol/CHANGELOG.md; compatibility smoke repeated; Appium 2 never"
  prequalification_outcome_exposure: true  # the frozen inputs above are immutable in response to the already-observed outcomes (quarantined Android emulator MQ3/MC-07 failure; local Z Flip 6 MQ3 failure); none of them was, or may be, adjusted because of what was observed

explicitly_excluded_as_formal_input:  # LOCAL PREFLIGHT CLOSURE instruction, section 14: these may remain documented as historical/prequalification context only and must not be imported or counted as formal gate observations
  note: "formal qualification under AUTH-02, once issued, starts a new execution lineage; it does not inherit or average with any of the items below"
  items:
    - "Samsung Galaxy Z Flip 6 local diagnostic results (qualification/mobilewright/local-preflight/android-zflip6/)"
    - "local iOS Simulator local diagnostic attempts (qualification/mobilewright/local-preflight/ios-simulator/, ios-simulator-retry/)"
    - "local preflight durations"
    - "the quarantined HISTORICAL_UNAUTHORIZED_EXECUTION qualification-labelled runs (qualification/mobilewright/output/, QUARANTINE-RECORD.yaml)"
    - "any other development/warm-up run outside a formally authorized and dispatched gate under this authorization_id"

if_issued_next_steps:  # not performed by this draft; listed so the human can see what issuance actually triggers. Frozen as an exact
                        # procedure (AUTH-02 PRE-ISSUANCE CONSISTENCY HARDENING instruction, section 7) to remove any ambiguity about
                        # promoting in place vs. creating a second record.
  gate_field_requirements: "the workflow's readiness gate (corrected 2026-09-23, CI_AUTHORIZATION_RESOLUTION_DEFECT; hardened 2026-09-23, AUTH-02 PRE-ISSUANCE CONSISTENCY HARDENING) discovers authorization records dynamically across manifests/mobile-qualification-execution-auth-v*.yaml and requires, on the uniquely id-matching record: authorization_type == MOBILE_QUALIFICATION_EXECUTION; record_validity explicitly present and not INVALID/SUPERSEDED/RETIRED (VALID); status == ISSUED exactly (absence now fails closed -- no longer permissively skipped); issued_by a real human identity, not empty/placeholder/agent-role; authorizes_execution exactly true (never defaulted); a frozen launch prompt that exists on disk"
  duplicate_record_prohibition: "issuance NEVER creates manifests/mobile-qualification-execution-auth-v2.yaml or any other second file carrying authorization_id: MOBILE-QUALIFICATION-EXEC-AUTH-02. The gate requires exactly one matching record; a second file with the same id would make that requirement fail (0 or 2+ matches is always closed). The ONLY correct promotion path is editing THIS file (manifests/mobile-qualification-execution-auth-v2-prepared.yaml) in place. Its filename does not need to change -- the gate discovers by authorization_id, not by filename convention -- though the human may rename it if they prefer; either way there is exactly one file for this id at all times."
  steps:
    - "1. the human explicitly issues MOBILE-QUALIFICATION-EXEC-AUTH-02 (an explicit reply; not inferred from preparation, discussion, or a request for recommendations -- section 8/16)"
    - "2. ORCHESTRATOR promotes THIS SAME prepared file in place (no second file is created; see duplicate_record_prohibition above)"
    - "3. set on this file: record_validity: VALID; status: ISSUED; issued_by: <the human's own identity as they state it, never an agent role>; issued_at: <the actual date>; authorizes_execution: true"
    - "4. a new frozen launch prompt is rendered for QUALIFIER-MOBILE-01 with INSTRUCTION_ID = MOBILE-QUALIFICATION-EXEC-AUTH-02 (prompts/README.md, rules 2-5), frozen no later than the first run"
    - "5. record on this file: launch_prompt.frozen_copy, launch_prompt.prompt_version_id, launch_prompt.sha256"
    - "6. ensure qualification/mobilewright/README.md records the same instruction id and the same prompt version id"
    - "7. publish the resulting clean provider-neutral export"
    - "8. only then is the GitHub Actions workflow (.github/workflows/e03-mobile-qualification.yml) dispatched with QUALIFICATION_INSTRUCTION_ID=MOBILE-QUALIFICATION-EXEC-AUTH-02 and the appropriate GATE_CONFIRM/candidate inputs; its readiness gate independently re-verifies every pin, the implementation-lock hashes, and every field requirement above before running anything"
    - "none of steps 2-8 happens until step 1 (the human's own explicit issuance reply) is received"

human_confirmation_requested: "Please reply explicitly if and when you wish to issue MOBILE-QUALIFICATION-EXEC-AUTH-02. Until then this remains PREPARED_AWAITING_EXPLICIT_HUMAN_AUTHORIZATION and authorizes nothing."
